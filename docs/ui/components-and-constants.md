# Evolution — UI: HUD, overlays and onboarding: component plan, acceptance, constants and the UI kit

§7–§10 of the split [`UI.md`](../UI.md), which keeps the shared context and the file list.

## 7. Angular component plan

Standalone, `OnPush`, signal inputs, no game logic: components format and lay out; every decision is a pure function
under `hud/format/` (or `state/` for the record, §3.1.4) with unit tests, and every fact comes from `GameStateService`
(the signal facade of `WorldStore`, architecture/client.md §5). Files ≤ 250 lines, one component per file
(`packages/client/src/app/game/hud/`). **This table is the one home of the HUD component and file list**:
architecture/client.md §6 points here and §10's file plan lists the same files without naming components.

| Component / file                                                                                                                                                               | Reads (signals)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Writes                                         | Owns test ids                                                                              |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `hud.component.ts` (shell, `hud`)                                                                                                                                              | `roundPhase` (the chrome's phase gate, §3.1.1) today; `lifeState`, `offer` and `openOverlay` as #188–#190 add the elements that need them; `ResizeObserver` on the host → `--hud-scale` (§1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | —                                              | `hud`, layout slots                                                                        |
| `own-cell-status.component.ts` (the status mirror, §3.1.4)                                                                                                                     | `ownCellIndicators`, `ownCell`, `ownProgress`, catalog                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | —                                              | `hud-own-cell`                                                                             |
| `leaderboard-panel.component.ts`                                                                                                                                               | `leaderboard`, `players`, `avatarAssignments`, `openOverlay`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `openOverlay` (`'leaderboard'`)                | `leaderboard*`                                                                             |
| `round-timer.component.ts`                                                                                                                                                     | `roundTimeLeftMs`, `sessionConfig`, `balance.session`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | —                                              | `hud-round-*`                                                                              |
| `trait-offer-overlay.component.ts`, `trait-card.component.ts`                                                                                                                  | `offer`, `serverTickEstimate`, `balance.traits`, catalog, `describeTierModifiers`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `previewTraitId`, `sendTraitChoice()`          | `trait-offer*`, `trait-card-*`                                                             |
| `respawn-overlay.component.ts`                                                                                                                                                 | `ownProgress`, `players`, `lastAliveOwnCell`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | —                                              | `respawn-*`                                                                                |
| `results-overlay.component.ts`                                                                                                                                                 | `leaderboard`, `players`, `resultsStartedAtTick`, `balance.session`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `leave()`                                      | `results-*`                                                                                |
| `menu-overlay.component.ts` (overlays.md §3.5)                                                                                                                                 | `openOverlay`, `ownProgress.ownedTraits`, `balance.traits`, catalog, `describeTierModifiers`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `openOverlay`, `openEncyclopedia()`, `leave()` | `menu-*`                                                                                   |
| `overlay-alert.component.ts` (encyclopedia.md §11.1; the menu renders it, the HUD shell projects it into the encyclopedia header)                                              | `ownCellIndicators`, `threats`, `ownProgress`, `serverTickEstimate`, through the pure `overlayAlertFor`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `openOverlay` (`'none'`)                       | the `testId` its host passes (`menu-alert`, `encyclopedia-alert`)                          |
| `game/encyclopedia/` (encyclopedia.md §11.7, its own file table)                                                                                                               | `EncyclopediaContextService`, `entriesIn`, `resolveEntry` (architecture/encyclopedia.md §12.2), `EncyclopediaStateService`; in a room `ownProgress`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `EncyclopediaStateService`, `openOverlay`      | `encyclopedia-*`                                                                           |
| `hint.component.ts`, `toast.component.ts`                                                                                                                                      | `OnboardingService.current`, `ToastService.current`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | —                                              | `hint`, `toast`                                                                            |
| `connection-banner.component.ts`                                                                                                                                               | `connectionState`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | —                                              | `connection-banner`                                                                        |
| `server-error-notice.component.ts`                                                                                                                                             | `serverError`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `dismissError()`                               | `hud-server-error*`                                                                        |
| `hud/format/*.ts` (pure)                                                                                                                                                       | `hudStyleVariables` (the `--hud-…` map, §1; the scale is the kit's `uiScaleFor`, §10.1), `formatRoundClock` / `roundClockStateFor` (the clock's whole record), `leaderboardEntriesFor` + `truncatePlayerName` (§3.1.1's cut and the own-row rule), `leaderboardSwatchFor` + `hexWithAlpha`, `describeTierModifiers(balance.traits, traitId, tier)` (the live tier tables and identity record, never the module constants, through `quantities/modifier-labels.ts`, whose label table is pinned against every `DEFAULT_CELL_MODIFIERS` key); every number the HUD shows as text (card lines, countdown, status sentence, clock, leaderboard cells) goes through `formatQuantity` (`game/quantities/`, `architecture/encyclopedia.md §12.5`), `threatsFor({ cells, ownCell, cameraExtent, players, balance })` (over the shared `canEngulf`, and over `isDiscVisibleInExtent` rather than the renderer's draw-cull `isDiscInExtent`, whose two-radius margin would name predators that are off screen), `seatMarkBeadCount` (over the shared `SEAT_MARK_BEADS`, `constants/lobby.ts` beside `AVATAR_INDEX_MAX`), `sprintFillFor`, `formatDnaLoss`, `formatOwnCellStatus` (the mirror's text and attributes) | —                                              | unit-tested, no DOM                                                                        |
| `state/own-cell-indicators.ts` (pure)                                                                                                                                          | `ownCellIndicatorsFor({ ownCell, ownProgress, balance, threats, previewTraitId })` → `OwnCellIndicators` (an input record, not a parameter list: the list ran past the lint bar, and the names read at the call site). `players` is not among them — the threat's name is resolved by `threatsFor`, which is where the roster is already in hand; `ladderFor(stage, traits, bacteriaEatenByVariant, previewTraitId)` (the requirement is the catalog's own `unlockedBy.count`, so no balance argument) (ghost silhouette by stage, counters, the preview hide rule); `LADDER_SILHOUETTE`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | —                                              | unit-tested, no DOM                                                                        |
| `hud/hud-state.service.ts`                                                                                                                                                     | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | the writable UI signals (below)                | —                                                                                          |
| `hud/onboarding.service.ts`, `hud/toast.service.ts`                                                                                                                            | `GameStateService`, effects                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `current`, `reticleVisible`                    | —                                                                                          |
| `hud/hud-constants.ts`, `game/test-ids/hud-test-ids.ts`, `game/glyphs/` (#312: `trait-glyphs.ts`, `trait-glyph.component.ts`, `glyph-constants.ts`; #391: `subject-glyphs.ts`) | §1's constants and §3.1.1's chrome sizes; `HUD_TEST_ID` (moved out of `hud/` by #361 so `hud/`, `input/` and `game/encyclopedia/` all import it from a leaf; with `leaderboardRowTestId` and `testIdSelector`) (`as const`, camelCase keys, the id strings of this doc as values); one inline SVG glyph per `TraitId` for the picker cards, the menu list and the encyclopedia (moved out of `hud/` so the encyclopedia never imports it; `game/glyphs/subject-glyphs.ts` beside it draws the other subjects), pinned by a test that every catalog id has one. `game/glyphs/` is #312's (its `subject-glyphs.ts` is #391's): `glyph-constants.ts` holds `TRAIT_GLYPH_LIST_PX` (20) and `TRAIT_GLYPH_CARD_PX` (56, the picker medallion), `TraitGlyphComponent` (`<app-trait-glyph [traitId] [lod]="'list' \| 'card'" [still]>`, `lod` defaulting to `'card'`) draws a trait, and `subject-glyphs.ts` draws every other encyclopedia subject on the same disc and rim                                                                                                                                                                                                                                      | —                                              | components and the Playwright loop both import `HUD_TEST_ID`; no id literal is typed twice |

Two services, two kinds of signal:

- `state/game-state.service.ts` (`GameStateService`, the facade over the client's one model, never a second one)
  holds only **derived** signals. Nothing the HUD reads is interpolated, so the round, roster, ranking, seats,
  session config and live balance are derived from the newest `game_state` / `game_snapshot` / `balance_updated`,
  which `MultiplayerService` already mirrors as signals; `WorldStore` stays the per-frame interpolated world the
  renderer draws, and the own-cell signals below read it through the render seam. `me` is this doc's shorthand;
  the signal is named `ownPlayerId` (the lint bar refuses a two-letter name). The signals: `me`, `ownCell`, `ownProgress`, `balance` (the snapshot's `game_state.balance`), `threats`,
  `ownCellIndicators` (§3.1.4; `null` while spectating), `connectionState`, `serverError`, `resultsStartedAtTick`,
  `serverTickEstimate`, `lastAliveOwnCell` (the own cell of the last snapshot with `lifeState === 'alive'`, kept
  until the next alive snapshot; §3.3) and `cameraExtent` (a world-space rectangle written by the render loop each
  frame through `game-setup.ts`; §3.1.2 `threatsFor` reads it, and it is the only render-side fact the HUD consumes).
- `hud/hud-state.service.ts` (`HudStateService`) holds the **writable** UI signals: `previewTraitId`
  (`TraitId | null`), `openOverlay` (`'none' | 'menu' | 'leaderboard' | 'encyclopedia'`; the full leaderboard is an
  overlay, so there is no separate expanded flag), `encyclopediaReturnTo` (`'menu' | 'game'`: where the
  encyclopedia's Escape goes, encyclopedia.md §11.1), `encyclopediaEntryId` (the entry `openEncyclopedia()` asked for,
  `trait:<traitId>` from a `Your traits` row, `null` for the last location), `menuReturnFocusTestId` (the menu control
  that opened the encyclopedia, which takes focus when its Escape brings the menu back) and the onboarding
  `reticleVisible` flag. `pressMenuKey()` is Escape's one close path: the topmost overlay closes, or the menu opens. #356's hold-Tab panel
  shows with the full leaderboard under `openOverlay = 'leaderboard'` and is not a value of its own, so Escape's
  topmost order already covers it.

Tab does not add a listener: `input/keyboard-input.ts` is the only keyboard handler on the document (§4), and its
controller reports the hold through `onFullLeaderboardHeldChanged`, which `game-host.component.ts` hands to
`HudStateService.setFullLeaderboardHeld`. A release with the leaderboard already closed leaves the state alone.

The HUD ↔ Pixi crossings are exactly four, all wired in `game-setup.ts` so that `render/` never imports from
`hud/` or `state/` services: the renderer receives `previewTraitId`, `reticleVisible` and `ownCellIndicators` as
read-only signals, and writes `cameraExtent`. Nothing else crosses; the renderer draws the indicators from the
record alone and computes only geometry (rendering/own-cell-indicators.md §10).

## 8. Acceptance (the Playwright loop #100 drives)

`OC` = `hud-own-cell` (§3.1.4). Pixel checks on the cell are graphics-qa's screenshot baselines (rendering/files-and-tests.md §9,
which gain the four sizes of §3.1.3), not this loop.

| #   | Steps                                                                                                                                                                         | Assert (by test id)                                                                                                                                                                                                                                                                                         |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| U1  | connect, create (seed 42, 60 s), start                                                                                                                                        | `game-canvas`, `hud`, `OC[data-level]` = 1, `OC[data-dna-percent]` = 0, `OC[data-ladder]` = `ghost:nucleoid`, `hud-round-clock` ≈ `1:00`, `hint[data-hint-id=steer]` visible                                                                                                                                |
| U2  | move the pointer 300 px right for 1 s                                                                                                                                         | steer hint gone, `OC[data-mass]` unchanged or larger, `leaderboard-row-<me>` present                                                                                                                                                                                                                        |
| U3  | `debug_grant_dna` 20                                                                                                                                                          | `trait-offer` visible with three `trait-card-*`, `trait-card-rung` on the nucleoid card (its `RUNG` ribbon)                                                                                                                                                                                                 |
| U4  | press `1`                                                                                                                                                                     | `trait-offer` gone within 2 snapshots, `OC[data-traits]` contains `nucleoid:1`, `OC[data-ladder]` = `counters`, `OC[data-aerobic]` = `0/10`, `OC[data-photosynthetic]` = `0/10`                                                                                                                             |
| U5  | `debug_spawn` a 200-mass cell adjacent, wait                                                                                                                                  | `OC[data-threat]` set, then `OC[data-engulfed]` set with `OC[data-engulf-phase]` = `cover`, then `absorb` (the seal), then `respawn-overlay` with `respawn-countdown` 3 → 1, then `OC[data-mass]` = starting mass and `OC[data-level]` kept                                                                 |
| U6  | hold Tab; press Escape; wait for the timer to reach zero                                                                                                                      | `leaderboard-full` while held, with `leaderboard-labels` reading `LV SCORE MASS ENGULFS` (decision #324); `menu-overlay` with focus on `menu-resume` and `menu-trait-nucleoid` listed; ids read from `HUD_TEST_ID`; `results-overlay` with `results-row-1`, then `hud` again after `RESULTS_SCREEN_SECONDS` |
| U7  | `debug_set_player` sprint on cooldown; wait                                                                                                                                   | `OC[data-sprint]` = `cooling`, then `ready`                                                                                                                                                                                                                                                                 |
| U8  | press Escape; activate `menu-encyclopedia`; press Escape twice                                                                                                                | `menu-overlay` with focus on `menu-resume`; `encyclopedia` with `encyclopedia-category-basics` selected; `menu-overlay` again with focus on `menu-encyclopedia`; no overlay, focus on `game-canvas`                                                                                                         |
| U9  | press `H`; activate `encyclopedia-category-evolutions`; press `/`, type `mito`, Enter; activate the first `encyclopedia-link-bacterium:aerobic`; activate `encyclopedia-back` | `encyclopedia` open; `encyclopedia-row-trait:nucleoid` listed; `encyclopedia-entry[data-entry-id="trait:mitochondrion"]` with `encyclopedia-preview`; then `bacterium:aerobic`; then `trait:mitochondrion` again                                                                                            |
| U10 | with the encyclopedia open: `debug_grant_dna` 20, press `1`; `debug_spawn` a 200-mass cell adjacent                                                                           | `encyclopedia-alert[data-alert-kind="offer"]`; `trait-offer` gone within 2 snapshots with `encyclopedia` still open; `encyclopedia-alert[data-alert-kind="threat"]`                                                                                                                                         |
| U11 | in the lobby activate `lobby-encyclopedia`; press Escape                                                                                                                      | `encyclopedia` with no `encyclopedia-alert`; focus back on `lobby-encyclopedia`                                                                                                                                                                                                                             |
| U12 | press Escape; activate `menu-exit`; press Escape; activate `menu-exit`, then `menu-exit-confirm`                                                                              | focus on `menu-exit-cancel`; focus back on `menu-exit`; the lobby screen                                                                                                                                                                                                                                    |

## 9. Constants table (own-cell indicators)

The values this doc owns (§3.1.2, §3.1.3, §6). Home: `packages/client/src/app/game/render/constants.ts`, the row
CODE-STANDARDS §2 gives them; the client's `render/constants-ledger.spec.ts` (#100) parses this table the way
`constants-ledger.test.ts` parses the four design tables (first-cell names, count pinned) against that file, so a
renamed floor fails on either side. Balance numbers the indicators read (`ENDOSYMBIOSIS_BACTERIA_REQUIRED` as the
endosymbionts' `unlockedBy.count` in `traits.TRAIT_CATALOG`, `ENGULF_SEAL_PROGRESS`, `SPRINT_COOLDOWN_SECONDS`,
`MAX_LEVEL`) are not here: they come from `game_state.balance`.

| Constant                                                    | Value      | Unit | Rationale                                                                                                                            |
| ----------------------------------------------------------- | ---------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `DNA_RING_RADIUS_FRACTION`                                  | 0.44       | × r  | Hugs the nucleus sprite's glow (0.40 r, rendering/contents-and-motion.md §3): "the nucleus halo" of option C.                        |
| `DNA_RING_MIN_RADIUS_PX`                                    | 17         | px   | The smallest ring whose inside holds a two-digit `value` numeral with its outline.                                                   |
| `DNA_RING_STROKE_PX`                                        | 4          | px   | The width of every chrome fill bar (picker timer); a 3 px arc was the failure in C.                                                  |
| `DNA_RING_TRACK_PAD_PX`                                     | 1          | px   | The track is `DNA_RING_STROKE_PX + 2 × pad` wide: it is also the ring's backing over the body.                                       |
| `DNA_RING_TRACK_ALPHA`                                      | 0.35       | ×    | Callout backing on the body: shows where 100 % is on every palette (1.5–2.1:1 against Mint).                                         |
| `DNA_RING_KEEP_OUT_FRACTION`                                | 0.66       | × r  | Organelle slot centres start here (rendering/contents-and-motion.md §3, every cell); clears the floored ring from 31 px up (§3.1.3). |
| `DNA_RING_KEEP_OUT_PAD_PX`                                  | 1          | px   | Ring edge to the nearest slot centre in the keep-out inequality.                                                                     |
| `LEVEL_NUMERAL_OUTLINE_PX`                                  | 2          | px   | Keeps the numeral ≥ 7:1 over the lit nucleus and the DNA fill.                                                                       |
| `LEVEL_NUMERAL_OUTLINE_ALPHA`                               | 0.70       | ×    | The outline's alpha in the callout-backing colour.                                                                                   |
| `SELF_RING_TRACK_ALPHA`                                     | 0.18       | ×    | The un-recharged remainder of the self ring (0.25 of `SELF_RING`'s 0.70): the identity tell never vanishes.                          |
| `LADDER_ORBIT_GAP_PX`                                       | 12         | px   | Beyond the self ring; clears its dash, its 20 °/s rotation and the seat mark's halo at every size (§3.1.3).                          |
| `LADDER_SEAT_MARK_CLEARANCE_PX`                             | 1          | px   | Backing inner edge to halo outer edge in the seat-mark inequality.                                                                   |
| `LADDER_GHOST_PX`                                           | 14         | px   | The smallest silhouette that still tells bean (cristae) from lens (granules) from loop.                                              |
| `LADDER_PIP_PX`, `LADDER_PIP_GAP_PX`                        | 4, 3       | px   | Pips countable at a glance (the seat mark's beads are countable at a 2 px radius; these are 2 px with gaps).                         |
| `LADDER_PIP_ROW_MAX`                                        | 5          | pips | Pips wrap into rows of five, so ten reads as a tally; two rows are 11 px across and fit the backing.                                 |
| `LADDER_PIP_STROKE_PX`                                      | 1          | px   | The unlit pip's outline.                                                                                                             |
| `LADDER_PIP_LIT_ALPHA`, `LADDER_PIP_UNLIT_ALPHA`            | 0.95, 0.70 | ×    | The lit / unlit tell besides the fill.                                                                                               |
| `LADDER_ITEM_GAP_PX`                                        | 4          | px   | Ghost to the pip block.                                                                                                              |
| `LADDER_ITEM_CLEARANCE_PX`                                  | 4          | px   | Drawn box to drawn box, a rung ghost to a counter sharing the orbit (#285 B): the counter turns away until this apart.               |
| `LADDER_BACKING_PX`                                         | 16         | px   | Callout backing under an orbit item (the ghost is 14 across, the pip block 11), so pips read over a bright zone tint.                |
| `LADDER_BACKING_END_PAD_PX`                                 | 4          | px   | Backing beyond the first and last item; as an angle it is `pad / orbitRadius`, so the two backings never overlap.                    |
| `LADDER_BACKING_ALPHA`                                      | 0.45       | ×    | The backing's alpha.                                                                                                                 |
| `LADDER_UNLOCK_RING_PAD_PX`, `LADDER_UNLOCK_RING_STROKE_PX` | 2, 1.5     | px   | The level-gold ring around a full counter's ghost.                                                                                   |
| `LADDER_ORBIT_ANGLE_SINGLE_DEG`                             | 180        | °    | A lone ghost sits at 6 o'clock, away from the seat mark's anchor (−135°) and the glint.                                              |
| `LADDER_ORBIT_ANGLES_PAIR_DEG`                              | 225, 135   | °    | Aerobic lower-left, photosynthetic lower-right; the backings keep ≥ 5° between them down to 24 px (§3.1.3).                          |
| `ESCAPE_ARC_STROKE_PX`                                      | 4          | px   | As the DNA ring.                                                                                                                     |
| `ESCAPE_ARC_TRACK_ALPHA`                                    | 0.20       | ×    | The danger track under the draining window; solid (1) once sealed.                                                                   |
| `THREAT_LABEL_GAP_PX`                                       | 6          | px   | Label pill to ring (threat label) and to arc (escape label).                                                                         |
| `LABEL_PILL_HEIGHT_PX`, `LABEL_PILL_PAD_PX`                 | 18, 8      | px   | The renderer's label pill: `label` role text with a full-height radius and 8 px at each end.                                         |
| `LABEL_PILL_ALPHA`                                          | 0.75       | ×    | The pill's callout backing over the dish.                                                                                            |
| `DANGER_LABEL_RIM_PX`                                       | 1          | px   | Danger rim around `WHITE` label text (§6): 19.4:1 measured, against 6.3:1 for danger text.                                           |
| `INDICATOR_FILL_TWEEN_MS`                                   | 200        | ms   | The DNA ring's fill tween (a rate, not a clip: it has no keyframes).                                                                 |

## 10. The UI kit (#354)

The look every panel, menu and screen copies, set once. The ESC menu ([`overlays.md §3.5`](./overlays.md#35-menu-escape))
and the encyclopedia ([`encyclopedia.md §11`](./encyclopedia.md#11-encyclopedia)) are its first users and #356's
hold-Tab panel is the next; the respawn, results and lobby screens move onto it by their own tickets. Home
`packages/client/src/app/ui-kit/`. The HUD elements built before it (the leaderboard, the picker cards, the notice
rows) keep their stylesheets until a ticket moves them, but no new screen grows its own panel, button or row. The
encyclopedia's layout is option B, the eyepiece (decision #368, encyclopedia.md §11.4); nothing in this section depended on it.

### 10.1 Tokens, and how components share them

- **One bridge.** A stylesheet cannot read a TypeScript constant, so the kit publishes its tokens as custom properties,
  the way layout.md §1 publishes the HUD's: `ui-kit/format/ui-css-variables.ts` → `uiStyleVariables()`, a pure map of
  every `--ui-…` property, pinned entry by entry and as an exact key set by its spec. It carries the type roles
  (`--ui-type-<role>` and `--ui-font-sans` / `--ui-font-mono`), the colour roles of
  visual-style/principles-and-palette.md §2 including the kit's (`--ui-text`, `--ui-panel-top`, `--ui-accent`, …) and
  every constant of the table below. Names follow one rule, so none is looked up: a constant drops `UI_` and `_PX`/`_MS` and is kebab-cased (`UI_RADIUS_PANEL_PX` → `--ui-radius-panel`), a colour role is kebab-cased (`PANEL_RIM` → `--ui-panel-rim`, `CALLOUT_BACKING` → `--ui-callout-backing`), and a type role is `--ui-type-<role>` beside `--ui-font-sans`, `--ui-font-mono` and `--ui-label-tracking`.
- **Surfaces.** `UiSurfaceDirective` (`[uiSurface]`) goes on **one element per full-viewport layer** (the HUD shell,
  the lobby's encyclopedia host), never on a panel or a kit component: it observes that host's size through the kit's
  one `ElementSizeTracker` (`element-size.ts`, which the HUD shell uses too, so the observer wiring exists once), sets `--ui-scale`
  from `uiScaleFor(width, height)` and spreads `uiStyleVariables()`, and a spec pins the scale for a viewport-sized
  host. `uiScaleFor` (`ui-kit/format/ui-scale.ts`, beside the `element-size.ts` observer) is layout.md §1's scale
  formula, moved into the kit with its four constants (`UI_REFERENCE_VIEWPORT_WIDTH_PX`, `UI_REFERENCE_VIEWPORT_HEIGHT_PX`,
  `UI_SCALE_MIN`, `UI_SCALE_MAX`, in `ui-kit-constants.ts` and not published as tokens), formula unchanged.
  Moving the existing HUD onto the kit is #381: the type and colour entries leave `hudStyleVariables`, the HUD
  stylesheets read `--ui-type-…`, the `--ui-` colours and `--ui-scale`, and `--hud-scale` goes, so one number has one
  name. Every kit length is `calc(<px> * var(--ui-scale))`; the focus ring alone is never scaled.
- **Boundaries.** Kit components are standalone and `OnPush`, with signal inputs and outputs. They import nothing from
  `@evolution/shared` and nothing from `game/**` except exactly the two constant files the colour and type roles live
  in, `game/render/constants/colours.ts` and `game/render/constants/ui-type.ts` (an ESLint `no-restricted-imports`
  rule on `ui-kit/**` allowlists those two paths),
  know no game words and read only `var(--ui-…)`. A feature passes its text and its test id in (a `testId` input,
  rendered on the root control), so every id keeps its one home (`HUD_TEST_ID`, `ENCYCLOPEDIA_TEST_ID`).

Constants, `ui-kit/ui-kit-constants.ts`, px at scale 1:

| Constant                                                                              | Value            | Unit | Meaning                                                                                                                                                           |
| ------------------------------------------------------------------------------------- | ---------------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `UI_SPACE_XS_PX`, `UI_SPACE_S_PX`, `UI_SPACE_M_PX`, `UI_SPACE_L_PX`, `UI_SPACE_XL_PX` | 4, 8, 12, 16, 24 | px   | The spacing scale; nothing is spaced off it.                                                                                                                      |
| `UI_PANEL_PADDING_PX`                                                                 | 24               | px   | A panel's inner padding.                                                                                                                                          |
| `UI_RADIUS_CONTROL_PX`                                                                | 4                | px   | Buttons, rows, fields and tables; the leaderboard's corner already.                                                                                               |
| `UI_RADIUS_PANEL_PX`                                                                  | 8                | px   | Panels and tiles.                                                                                                                                                 |
| `UI_RIM_PX`                                                                           | 1                | px   | Every rim: a panel's, a control's, a key hint's.                                                                                                                  |
| `UI_BUTTON_HEIGHT_PX`                                                                 | 40               | px   | A button.                                                                                                                                                         |
| `UI_BUTTON_COMPACT_HEIGHT_PX`                                                         | 28               | px   | A compact button, a segmented switch, a control over a preview.                                                                                                   |
| `UI_BUTTON_PADDING_INLINE_PX`                                                         | 16               | px   | A button label's inset.                                                                                                                                           |
| `UI_ROW_HEIGHT_PX`                                                                    | 40               | px   | A list row.                                                                                                                                                       |
| `UI_RAIL_ROW_HEIGHT_PX`                                                               | 36               | px   | A rail item.                                                                                                                                                      |
| `UI_ROW_MEDALLION_PX`                                                                 | 24               | px   | A row's leading medallion.                                                                                                                                        |
| `UI_CARD_MEDALLION_PX`                                                                | 56               | px   | A card's medallion: the picker card's and an encyclopedia tile's, the size of `TRAIT_GLYPH_CARD_PX`.                                                              |
| `UI_ROW_MARKER_PX`                                                                    | 8                | px   | A facts-table row's leading marker when it is a dot or ring; the slot takes its content's width, so a glyph marker sits in it at `TRAIT_GLYPH_LIST_PX`.           |
| `UI_ROW_MARKER_RING_PX`                                                               | 2                | px   | A ring marker's stroke.                                                                                                                                           |
| `UI_FACT_ROW_HEIGHT_PX`                                                               | 26               | px   | A facts-table row.                                                                                                                                                |
| `UI_SELECTION_BAR_PX`                                                                 | 3                | px   | The accent bar on a selected rail item, row or tab.                                                                                                               |
| `UI_ROW_HOVER_ALPHA`                                                                  | 0.06             | ×    | The hover tint, in the text colour.                                                                                                                               |
| `UI_ROW_SELECTED_ALPHA`                                                               | 0.12             | ×    | The selected tint, in the accent.                                                                                                                                 |
| `UI_ROW_PRESSED_ALPHA`                                                                | 0.18             | ×    | The pressed tint, in the text colour.                                                                                                                             |
| `UI_WELL_ALPHA`                                                                       | 0.45             | ×    | The callout backing inside a panel: the rail, fields, key hints, controls over a preview.                                                                         |
| `UI_DISABLED_ALPHA`                                                                   | 0.45             | ×    | A disabled control's opacity.                                                                                                                                     |
| `UI_CHIP_HEIGHT_PX`                                                                   | 20               | px   | A chip.                                                                                                                                                           |
| `UI_CHIP_PADDING_INLINE_PX`                                                           | 8                | px   | A chip is its text (and dot) plus this at each end; no chip has a fixed width.                                                                                    |
| `UI_CHIP_DOT_PX`                                                                      | 7                | px   | The tone dot of a chip or an alert pill.                                                                                                                          |
| `UI_BUTTON_COMPACT_PADDING_INLINE_PX`                                                 | 12               | px   | A compact button or link chip is its label plus this at each end.                                                                                                 |
| `UI_ALERT_HEIGHT_PX`                                                                  | 28               | px   | The alert strip's pill.                                                                                                                                           |
| `UI_DANGER_RIM_ALPHA`                                                                 | 0.7              | ×    | The danger button's rim: 3.17:1 against `PANEL_TOP`, over the 3:1 bar for a control's boundary.                                                                   |
| `UI_PRIMARY_FILL_ALPHA`, `UI_PRIMARY_RIM_ALPHA`                                       | 0.16, 0.7        | ×    | The primary button's fill and rim, in the accent.                                                                                                                 |
| `UI_SECONDARY_FILL_ALPHA`                                                             | 0.04             | ×    | The secondary and icon buttons' fill, in the text colour.                                                                                                         |
| `UI_PANEL_EDGE_ALPHA`                                                                 | 0.05             | ×    | A modal panel's top edge, in `WHITE`.                                                                                                                             |
| `UI_SCROLL_THUMB_ALPHA`                                                               | 0.55             | ×    | The scrollbar thumb: `TEXT_MUTED` at this share mixed into the panel rim, which alone is lost on the well; 3.18:1 on `PANEL_TOP`, over the 3:1 bar for a control. |
| `UI_SIDE_PANEL_WIDTH_PX`                                                              | 360              | px   | A `side` panel's width.                                                                                                                                           |
| `UI_SIDE_PANEL_ALPHA`                                                                 | 0.86             | ×    | A `side` panel's gradient opacity over the live dish.                                                                                                             |
| `UI_SIDE_PANEL_BLUR_PX`                                                               | 8                | px   | The backdrop blur under a `side` panel.                                                                                                                           |
| `UI_KEY_HINT_HEIGHT_PX`                                                               | 18               | px   | A keycap.                                                                                                                                                         |
| `UI_SEARCH_WIDTH_PX`, `UI_SEARCH_HEIGHT_PX`                                           | 280, 32          | px   | The search field.                                                                                                                                                 |
| `UI_SCROLLBAR_PX`                                                                     | 8                | px   | The scrollbar gutter.                                                                                                                                             |
| `UI_SCROLL_FADE_PX`                                                                   | 16               | px   | The fade at a scroll area's edge while content lies beyond it.                                                                                                    |
| `UI_FOCUS_RING_PX`                                                                    | 2                | px   | Every control's focus ring, in the text colour, never scaled; `HUD_FOCUS_RING_PX` becomes this constant.                                                          |
| `UI_FOCUS_RING_OFFSET_PX`                                                             | 2                | px   | The ring sits this far outside the control, or inside it where a scroll area would clip it (rows, rail).                                                          |
| `UI_TRANSITION_MS`                                                                    | 120              | ms   | Hover, press and selection fades.                                                                                                                                 |
| `UI_PANEL_ENTER_MS`                                                                   | 160              | ms   | A modal panel fades in rising `UI_SPACE_S_PX`, its scrim fading with it; a `side` panel has no enter motion.                                                      |
| `UI_BODY_LINE_HEIGHT`                                                                 | 1.45             | ×    | A panel body's line height: prose, list rows and a confirm's two lines all sit in it.                                                                             |

### 10.2 Components

| Component (`ui-kit/`)                                                              | Selector                                         | Inputs and outputs                                                                                                                                             | What it is                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------------------------------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ui-surface.directive.ts`                                                          | `[uiSurface]`                                    | —                                                                                                                                                              | §10.1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ui-panel.component.ts`                                                            | `ui-panel`                                       | `variant: 'modal' \| 'side'`, `title`, `subtitle`; slots `[uiPanelHeader]`, the body, `[uiPanelBleed]`, `[uiPanelFooter]`                                      | `modal`: the opaque panel gradient (`PANEL_TOP` → `PANEL_BOTTOM`), a 1 px `PANEL_RIM` rim, a `WHITE` @ 5 % top edge, `UI_RADIUS_PANEL_PX`, `UI_PANEL_PADDING_PX`; `role="dialog"`, `aria-modal`, labelled by its title; the host places it and it enters over `UI_PANEL_ENTER_MS`. Either variant's body sets `UI_BODY_LINE_HEIGHT`, so prose, rows and a confirm's lines are spaced once. `side`: `UI_SIDE_PANEL_WIDTH_PX` wide and at most the viewport height less two `HUD_MARGIN_PX`, its body a kit scroll area; the host anchors it to a viewport edge at `HUD_MARGIN_PX` (the kit never places it); the panel gradient at `UI_SIDE_PANEL_ALPHA` over a `UI_SIDE_PANEL_BLUR_PX` backdrop blur of the dish; `UI_SPACE_L_PX` padding, sections under `label` headings `UI_SPACE_M_PX` apart with a 1 px `PANEL_RIM` rule between them; it appears and disappears at once, with no fade and no rise, because it shows while a key is held; no scrim and no trap, `role="region"`, taking the pointer only on its controls. #356's hold-Tab panel is one (`kit-states-1280x800.png` draws a sample). |
| `ui-panel-section.component.ts`                                                    | `ui-panel-section`                               | `heading`                                                                                                                                                      | A panel section, `role="group"` under its `label` heading: `UI_SPACE_M_PX` after the section before it with a 1 px `PANEL_RIM` rule between them (the `side` panel's sections).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `ui-scrim.component.ts`                                                            | `ui-scrim`                                       | `alpha`                                                                                                                                                        | The callout backing over the viewport; it takes the pointer so a click never reaches the dish, and closes nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `ui-focus-trap.directive.ts`                                                       | `[uiFocusTrap]`, `[uiAutofocus]`                 | `restoreTo`                                                                                                                                                    | Holds Tab and Shift+Tab inside its host, focuses the `[uiAutofocus]` element on open and restores focus on destroy. Traps **stack**: a document-level stack in which only the topmost trap handles Tab, and destroying a trap restores its focus and pops it (the menu over the results overlay, the encyclopedia replacing the menu); a spec covers a trap over a trap. `FocusReturn` (`ui-kit/focus-return.ts`) sits beside it as the non-trapping variant the picker keeps. The trap listens on its own host, so `input/keyboard-input.ts` stays the one document keyboard handler; a trap with nothing focusable holds focus on its host. Focus that leaves the topmost trap on its own — a click on the scrim or on a panel's text drops it on `<body>` — is sent back inside on `focusout`, so the next Tab cannot reach the page behind the modal; a trap under another never pulls focus back.                                                                                                                                                                                                  |
| `ui-button.component.ts`                                                           | `button[uiButton]`                               | `variant: 'primary' \| 'secondary' \| 'danger' \| 'quiet' \| 'icon'`, `size: 'regular' \| 'compact'`, `keyHint`, `isDisabled`                                  | Its width is its label (and key hint, `UI_SPACE_M_PX` after it) plus `UI_BUTTON_PADDING_INLINE_PX`, or `UI_BUTTON_COMPACT_PADDING_INLINE_PX` when compact, unless the host sets a full width. Primary: accent @ `UI_PRIMARY_FILL_ALPHA` fill, accent @ `UI_PRIMARY_RIM_ALPHA` rim, bold label. Secondary: text colour @ `UI_SECONDARY_FILL_ALPHA` fill, panel-rim rim. Danger: no fill, `DANGER` @ `UI_DANGER_RIM_ALPHA` rim, label in the text colour (the rim warns, the words say what happens). Quiet: label colour, no rim. Icon: square, the secondary fill and rim, the label colour. A pressed quiet or icon label takes the text colour: the label colour over the pressed tint is exactly 4.5:1, with no margin. `keyHint` is a `KeyboardEvent.key` name: it renders a key hint (`Escape` shows `ESC`, the arrows as arrows) and `aria-keyshortcuts`. `isDisabled` sets `aria-disabled` and swallows the press; the button keeps its Tab stop.                                                                                                                                                |
| `ui-key-hint.component.ts`                                                         | `ui-key-hint`                                    | `key`                                                                                                                                                          | A keycap: `caption`, muted, on the well with a panel-rim rim; `aria-hidden`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `ui-chip.component.ts`                                                             | `ui-chip`, `a[uiLinkChip]`, `button[uiLinkChip]` | `tone: 'muted' \| 'neutral' \| 'strong' \| 'accent' \| 'gold' \| 'danger' \| 'dna'`, `dotColour`                                                               | A static pill (`label`, uppercase, on the well), its rim and text in its tone with an optional `UI_CHIP_DOT_PX` dot in a colour role, its text plus `UI_CHIP_PADDING_INLINE_PX` at each end. Rarity takes `muted` (common), `strong` (uncommon) and `dna` (rare), the rarity colours of principles-and-palette.md §2; a DNA tag is `neutral` with its tag dot, an owned tier `gold`; `danger` keeps its words in the text colour; `accent` is for a selection only. The link chip is interactive: a compact secondary button in the link colour (it wears `ui-button.component.css`, so it has every button state), its title plus `UI_BUTTON_COMPACT_PADDING_INLINE_PX`.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ui-alert-pill.component.ts`                                                       | `button[uiAlertPill]`                            | `tone: 'danger' \| 'gold'`, `figure`, `keyHints`                                                                                                               | The alert strip (encyclopedia.md §11.1), `UI_ALERT_HEIGHT_PX` tall and as round as it is high: `label` in `WHITE`, uppercase, with any changing number (`figure`) in `figure`, a `UI_CHIP_DOT_PX` tone dot and a 1 px tone rim, on the callout backing; optional trailing key hints (`keyHints`), `UI_SPACE_XS_PX` apart as one set. In a column narrower than its words (a wide fallback face) the label gives way with an ellipsis; the figure and the keys never do. It wears the button's stylesheet for its states.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ui-medallion.component.ts`                                                        | `ui-medallion`                                   | `size: 'row' \| 'card'`, `tone: 'neutral' \| 'gold'`                                                                                                           | A round well with a panel-rim rim for a mark with no disc of its own (a subject icon, a still frame, a tier numeral in `label`), `UI_ROW_MEDALLION_PX` in a row or `UI_CARD_MEDALLION_PX` on a card; `gold` takes the level-gold rim and mark (an owned tier). A trait glyph (#312) draws its own disc and rim and is placed bare.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `ui-rail.component.ts`, `ui-rail-item.component.ts`                                | `ui-rail`, `ui-rail-item`                        | `orientation`, `selectedId` / `selectedIdChange`; an item's `itemId`, `count`, `isDisabled`, `[uiLeading]` icon                                                | `role="tablist"`, one Tab stop with a roving tabindex (`roving-group.ts`, shared with the list), selection follows focus, ↑ ↓ (← → when horizontal; the other pair passes through, so a feature can move between columns), Home End, Enter or Space; a move skips a disabled item and stops at either end. An item is `UI_RAIL_ROW_HEIGHT_PX`, its label `TEXT_LABEL` at rest and the text colour while hovered or selected, its count `figure` in `TEXT_MUTED` at rest and `TEXT_LABEL` while hovered or selected (3.92:1 on the selected tint otherwise, under 4.5:1), and the text colour while pressed (the label colour over selected and pressed is 3.82:1). A horizontal item is sized to its label, its bar on the bottom edge. The id input is `itemId`, never `id`, which would stamp a DOM id two rails could share.                                                                                                                                                                                                                                                                         |
| `ui-list.component.ts`, `ui-list-row.component.ts`, `ui-list-section.component.ts` | `ui-list`, `ui-list-row`, `ui-list-section`      | `selectedId` / `selectedIdChange`, `shouldSelectionFollowFocus`; a row's `itemId`, `isDisabled`, `[uiLeading]` and `[uiTrailing]` slots; a section's `heading` | `role="listbox"` with `role="option"` rows, one Tab stop with a roving tabindex through every section's rows in document order, ↑ ↓ Home End (no wrap, disabled rows skipped), Enter or Space selects; with `shouldSelectionFollowFocus` (off by default) arrowing selects as it goes. A row is `UI_ROW_HEIGHT_PX`, its title `body` in the text colour ending in an ellipsis; a section is a `role="group"` under a `label` header. Rail items and rows share `ui-row-states.css`: square, so a column reads as one surface, with the states below and the focus ring inside the row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ui-scroll-area.component.ts`                                                      | `ui-scroll-area`                                 | `label`                                                                                                                                                        | `overflow: auto` with a thin themed scrollbar (`UI_SCROLLBAR_PX` on the well, its thumb the panel rim lifted by `TEXT_MUTED` at `UI_SCROLL_THUMB_ALPHA`) and a `UI_SCROLL_FADE_PX` mask fade on each edge that has content beyond it, re-read on scroll and on resize (`measure()` for a feature that changes content without resizing it); a `role="region"` Tab stop named by `label` only while it overflows and holds nothing focusable, its ring inside. While it overflows its viewport takes the pointer, so the wheel scrolls a `side` panel body that otherwise lets the pointer through. Every panel body is one, named by the panel's `title`. The page itself never scrolls.                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ui-facts-table.component.ts`                                                      | `ui-facts-table`                                 | `rows`, `columns`, `columnsCaption`, `highlightColumn`; `ng-template[uiFactMarker]`, `ng-template[uiFactValue]`                                                | A real `<table>`, names as row headers: an optional leading marker column as wide as its content, a row's `marker` (a `UI_ROW_MARKER_PX` dot, or a ring stroked `UI_ROW_MARKER_RING_PX`, in a colour role) or the feature's `uiFactMarker` template (a glyph at `TRAIT_GLYPH_LIST_PX`); the name always `UI_SPACE_S_PX` after it, names in `body` and the label colour, values in `figure` right-aligned, a panel-rim rule under each `UI_FACT_ROW_HEIGHT_PX` row but the last in a panel section, where the section rule follows; a `uiFactValue` template draws a value that is more than text (a link). With columns, a `label` header row (`columnsCaption` over the names, in `TEXT_MUTED` at normal weight) and the `highlightColumn` tinted `UI_SELECTED`, its header in the accent.                                                                                                                                                                                                                                                                                                             |
| `ui-search-field.component.ts`                                                     | `ui-search-field`                                | `query` (model), `placeholder`, `keyHint`                                                                                                                      | A native `input type="search"` named by its placeholder, on the well (`UI_SEARCH_WIDTH_PX` × `UI_SEARCH_HEIGHT_PX`) with a search icon and a key hint (`aria-keyshortcuts`); the ring is drawn on the field while its input shows focus. Escape clears a non-empty query and stops there (`preventDefault`, `stopPropagation`), else passes through. It edits `query` only: what matches is the feature's.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

**States**, for every interactive kit element (`qa/decisions/encyclopedia/kit-states-1280x800.png` draws each one):

| State    | Look                                                                                                                                                                                                        |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Rest     | The variant's fill and rim.                                                                                                                                                                                 |
| Hover    | `UI_ROW_HOVER_ALPHA` of the text colour over it. A pointer never moves focus.                                                                                                                               |
| Pressed  | `UI_ROW_PRESSED_ALPHA` instead.                                                                                                                                                                             |
| Selected | Rail items, rows, tabs and tier-switch segments: `UI_ROW_SELECTED_ALPHA` of the accent, a `UI_SELECTION_BAR_PX` accent bar on the leading edge (the bottom edge for a tab or a segment), text colour, bold. |
| Focus    | `:focus-visible` only: the `UI_FOCUS_RING_PX` ring in the text colour at `UI_FOCUS_RING_OFFSET_PX`, drawn over any other state.                                                                             |
| Disabled | `UI_DISABLED_ALPHA` opacity, `aria-disabled`, no hover.                                                                                                                                                     |

Fades take `UI_TRANSITION_MS`. Under `prefers-reduced-motion` nothing transitions and panels appear without rising.

A development build opened with `?kit` renders the kit states page (`ui-kit/kit-states/`): every button variant in
every state, the sizes and key hints, a `side` panel whose sections are facts tables in its scroll area, a tier
table, and a modal over a scrim in a focus trap, on one `[uiSurface]`. `?kit&sheet=collections` renders the
collections sheet: the rail item and list row in every state, a horizontal rail, the chips, link chips, medallions
and alert pills, and a live search field over a rail and a sectioned list, each in a scroll area.
Graphics review screenshots both sheets at 1280 × 800 against `kit-states-1280x800.png`, forcing hover, pressed and focus
through the browser rather than with classes; production builds never render it.

### 10.3 Colour and type

No new hue and no new type role: the kit's colour roles are existing colours at the alphas above, named in
[`visual-style/principles-and-palette.md §2`](../visual-style/principles-and-palette.md#2-palette), and which type role each
kit part takes is [`visual-style/ui-type.md §7`](../visual-style/ui-type.md#7-ui-colours-and-type).
