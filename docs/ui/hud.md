# Evolution — UI: HUD, overlays and onboarding: in-round HUD and own-cell indicators

§3–§3.1 of the split [`UI.md`](../UI.md), which keeps the shared context and the file list.

## 3. In-round HUD and overlays

Every element: source, placement, size, states, text, test id. Text is short and literal (values, not sentences)
except hints and toasts. Numbers are integers; masses round down.

### 3.1 In-round elements (visible while `roundPhase === 'playing'` and `lifeState === 'alive'`)

Option C has two kinds of in-round element. **Chrome** (§3.1.1) is DOM in the corners: the leaderboard and the
round clock, nothing else. **Own-cell indicators** (§3.1.2) are drawn on and around the player's cell by the
renderer from one record the HUD derives (`OwnCellIndicators`, §3.1.4); the level ring, mass readout, DNA line,
ladder hint, minimap, trait strip, sprint meter, key hints and danger chip of the pre-#146 spec are gone, and
their facts live on the cell, in the full leaderboard (mass, §3.1.1), in the menu's trait list (§3.5) and in the
status mirror (§3.1.4) that tests and assistive technology read.

#### 3.1.1 Chrome

| Element     | Source                                                                                                                                                          | Placement, size                                                                                                        | States and text                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `data-testid`                                                                                                                     |
| ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| Leaderboard | `snapshot.leaderboard`, names from `players[id].playerName`, swatches from `mp.avatarAssignments()`; the full list adds `LeaderboardRow.mass` and `absorptions` | top-right (16,16), 240 × (26 header + 16 label strip + 24 per row), max 5 rows compact; the full list adds a 24 footer | Columns rank, swatch 10 px, name (ellipsis at 12 chars), level, score in `body`. The swatch is the palette base with a rim-colour ring and `seatMarkBeadCount(avatarIndex)` beads (`hud/format/seat-mark.ts`, §7) = `SEAT_MARK_BEADS[avatarIndex]` = `avatarIndex + 1`, from the shared constant the renderer's seat mark reads (visual-style/principles-and-palette.md §2), so a player can match dish to board; the results table (§3.4) reuses the same swatch. Own row tinted (visual-style/ui-type.md §7) and always present: if outside the top 5 it replaces row 5; **it is where the level is always legible as text**. Header `LEADERBOARD` in `caption` with `HOLD TAB` right-aligned in `caption` muted (`TAB HELD` while the full list is open). A `caption` **label strip** (`LEADERBOARD_LABEL_ROW_HEIGHT_PX` 16) sits under the header on both panels (decision #324): `LV SCORE` on the compact panel, `LV SCORE MASS ENGULFS` on the full one. The #321 audit found the unlabelled score read as nothing and `EATEN` read as food, so the column "never moved"; it counts cells engulfed and is named for that. **Tab held** (or the header clicked) sets `openOverlay = 'leaderboard'` and expands to the full list: up to 8 rows, 360 wide, adds the mass and engulfs columns and a footer row (`LEADERBOARD_FOOTER_ROW_HEIGHT_PX`) in `label`, `SCORE = DNA + 25 PER ENGULF · KEPT ON DEATH` (25 = `balance.session.SCORE_ABSORPTION_BONUS`, formatted, never typed); the hold-Tab panel (overlays.md §3.7) opens with it. Rows re-sort with a 200 ms slide. Columns are **fixed** tracks, not `auto`, and the label strip is laid out from the same track list, so a label always sits over the column it names. Rank, name, level and score are `body`; the three numeric columns are `figure` (visual-style/ui-type.md §7: `body`'s size in the mono face, so the digits are tabular at a 24-px row); the label strip is `caption`. **Phase and life:** the board is the one in-round element exempt from §3.1's `lifeState === 'alive'` half — a dead player watching their killer is exactly who wants the ranking — but it does stand down for `results`, where the results overlay (§3.4) claims the same corner. | `leaderboard`, `leaderboard-header`, `leaderboard-labels`, `leaderboard-row-<playerId>`, `leaderboard-full`, `leaderboard-footer` |
| Round timer | `snapshot.roundTimeLeftMs`, `mp.sessionConfig().roundDurationSeconds`, `balance.session.ROUND_BLOOM_START_FRACTION`                                             | bottom-right (16,16); `clock` role `07:42`, `ROUND` in `caption` under it                                              | Formatted `m:ss` floor. In bloom the clock turns level gold and the caption says the effect, `BLOOM · FOOD ×1.5 · DNA DROPS ×2`, in `label` (it carries a fact, so not `caption`), right-aligned under the clock; the multipliers are `balance.ecology.FOOD_BLOOM_SPAWN_MULTIPLIER` and `DNA_FRAGMENT_BLOOM_SPAWN_MULTIPLIER` through `roundClockStateFor`, never typed (decision #324). Last 10 s: pulses once per second. `results` phase: hidden (the results overlay shows the countdown).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `hud-round-clock`, `hud-round-phase`                                                                                              |

The chrome's phase gate lives in `hud.component.ts`, which is the only component that reads `roundPhase` for both.

There is no key-hint line: Tab is printed on the leaderboard header, Space is taught by the `sprint` onboarding
beat (§5; the pill reads `TAP sprint` on touch devices), Escape is not taught.

#### 3.1.2 Own-cell indicators (what is shown; rendering/own-cell-indicators.md §10 owns how)

All six live in the own cell's undeformed frame (they never bend with the membrane) and follow the cell's
on-screen radius `r_px` with the floors of §3.1.3; every constant named here is a row of §9. Angles are clockwise
from 12 o'clock. Colours are roles; timings are rendering/contents-and-motion.md §4 clips or §9 rates, never a bare number.

| Indicator         | Source (architecture/entity-model.md §2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Placement, size                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | States                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **DNA ring**      | `ownProgress.dnaTowardNextLevel`, `levelUpCost(level, balance.progression)` (shared `simulation/level-costs.ts`, PROGRESSION §2), `balance.progression.MAX_LEVEL`; the `level_up` effect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | the nucleus halo: a ring centred on the cell at radius `max(DNA_RING_RADIUS_FRACTION × r_px, DNA_RING_MIN_RADIUS_PX)`, stroke `DNA_RING_STROKE_PX`, always inside the body (organelle slot centres keep outside `DNA_RING_KEEP_OUT_FRACTION`, rendering/contents-and-motion.md §3); a **track** in the callout-backing colour at `DNA_RING_TRACK_ALPHA`, `DNA_RING_TRACK_PAD_PX` wider than the fill on each side, so it shows where 100 % is and backs the fill over whatever lies under it (dark on every palette: measured 1.5–2.1:1 against the brightest body, Mint, on the size sheet); the fill an arc from 12 o'clock clockwise in the DNA colour, round caps                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `dnaFraction = dnaTowardNextLevel / levelUpCost`, tweened over `INDICATOR_FILL_TWEEN_MS`. At `MAX_LEVEL` the ring is full in level gold. On `level_up` the ring flashes gold with the clip's `ringFlash` track (rendering/contents-and-motion.md §4: one clock, the renderer's `renderTick`, so the flash and the burst start on the same frame), then the new, near-empty fill shows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Level numeral** | `ownProgress.level`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | the ring's centre; `value` role, text colour `WHITE`, on a `LEVEL_NUMERAL_OUTLINE_PX` outline in the callout-backing colour at `LEVEL_NUMERAL_OUTLINE_ALPHA` so it reads over a lit nucleus; never scales                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `1` … `12`; the ring floor (§3.1.3) is chosen so two digits fit inside it. Flashes with the ring (`ringFlash`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Ladder orbit**  | `ownCell.stage`, `STAGE_GATE_TRAITS` (`constants/ladder.ts`), `ownProgress.bacteriaEatenByVariant`, the endosymbiont's own `unlockedBy.count` in `TRAIT_CATALOG` (the same number the server's draft gate reads, and its only copy on the wire: `balance.ladder` carries no count, #286), `ownCell.traits`, `previewTraitId` (§7)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | an arc band outside the identity ring: radius `selfRingPx + LADDER_ORBIT_GAP_PX` (`selfRingPx` = visual-style/principles-and-palette.md §2's `SELF_RING_RADIUS_FRACTION × r_px` with its `SELF_RING_MIN_PX` floor; the gap clears the seat mark's halo at every size, §3.1.3). Items lie along the arc on a callout-backing arc `LADDER_BACKING_PX` wide at `LADDER_BACKING_ALPHA`, extended `LADDER_BACKING_END_PAD_PX` past the first and last item (a px pad whose angle comes from the radius, so it is the same 4 px at every size). A **ghost** is the dashed silhouette of what the next rung draws, keyed by `LadderSilhouette` (§3.1.4: `nucleoid` loop, `envelope` circle with pores, `form` slipper outline) or, on a counter, by its organelle (mitochondrion bean with cristae, chloroplast lens with granules), `LADDER_GHOST_PX` long, tangent to the arc, in its organelle colour (rim colour for nucleus parts). A counter's **pips** are `ENDOSYMBIOSIS_BACTERIA_REQUIRED` pips in rows of `LADDER_PIP_ROW_MAX` (two rows of five at the current 10, counted the way a tally is), ⌀ `LADDER_PIP_PX`, gap `LADDER_PIP_GAP_PX` between pips and between rows, straight along the tangent `LADDER_ITEM_GAP_PX` after the ghost, the row nearest the cell first; one atlas entry per (variant, eaten), so a counter is two sprites (rendering/own-cell-indicators.md §10) | Per stage. `protocell` → ghost `nucleoid` at `LADDER_ORBIT_ANGLE_SINGLE_DEG` 180. `prokaryote` → two **counters** at `LADDER_ORBIT_ANGLES_PAIR_DEG` (aerobic 225, photosynthetic 135): ghost, then pips lit clockwise in the organelle colour at `LADDER_PIP_LIT_ALPHA` (unlit: a `LADDER_PIP_STROKE_PX` outline at `LADDER_PIP_UNLIT_ALPHA`) as `bacteriaEatenByVariant[variant]` grows; at `required` the ghost gains a level-gold ring `LADDER_UNLOCK_RING_PAD_PX` outside it, `LADDER_UNLOCK_RING_STROKE_PX` wide, until the trait is picked (this is the counter gameplay-qa flagged: without it the vent trip is a hidden requirement). An owned endosymbiont hides its counter; the other keeps its angle and stays on the orbit through `endosymbiosis`, `eukaryote` and `specialised`, beside that stage's ghost, until it is owned too (decision #285 B: the draft offers an endosymbiont from `prokaryote` on). Where a ghost shares the orbit, the ghost keeps its angle and the counter turns away from it (aerobic clockwise, photosynthetic counter-clockwise) just far enough that the drawn boxes stay `LADDER_ITEM_CLEARANCE_PX` apart (§3.1.3). `endosymbiosis` → ghost `envelope` at 180; `eukaryote` → ghost `form` at 180 (`STAGE_GATE_TRAITS.specialised` holds five forms, so the ghost is keyed by the next stage's silhouette, never by one trait); `specialised` → no ghost. While the picker (§3.2) previews a card whose trait is in `STAGE_GATE_TRAITS[nextStage]`, the ghost hides (on a counter, only the previewed endosymbiont's ghost; its pips stay): the preview shows the real organelle. |
| **Sprint ring**   | `ownCell.sprintCooldownRemainingTicks` (no client estimate), `ownCell.sprintRemainingTicks`, `balance.controls.SPRINT_COOLDOWN_SECONDS`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **the identity ring itself** (visual-style/principles-and-palette.md §2's self ring: its radius, dash, width, rotation and colour are unchanged)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Always drawn: the recharged part at the self ring's alpha and the remainder as a track at `SELF_RING_TRACK_ALPHA`, so the dashed circle (the own cell's non-colour tell, visual-style/principles-and-palette.md §2) is present at every fill, the frame after a sprint included. Ready (`sprintCooldownRemainingTicks === 0`): the full ring. Cooling: the ring is drawn as an arc from 12 o'clock to `sprintFill` = `1 − remaining / secondsToTicks(SPRINT_COOLDOWN_SECONDS)` clamped to [0, 1] (a cooldown shortened by a folded modifier starts partly drawn). Sprinting (`sprintRemainingTicks > 0`): full ring at the sprint rim brightness (visual-style/motion-and-legibility.md §5). Reaching ready: the `sprint_ready` clip (rendering/contents-and-motion.md §4), no sound of its own.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Escape arc**    | `ownCell.states` contains `being_engulfed`, `ownCell.engulfProgress`, `ownCell.engulfedByCellId`, `engulfPhaseOf(engulfProgress, balance.absorption)` (shared `simulation/engulf-pace.ts`, ecology/absorption.md §6.1, the function the doc names for "showing the phase")                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | replaces the ladder orbit on its radius while `being_engulfed`: a danger-colour arc, stroke `ESCAPE_ARC_STROKE_PX`, on a danger track at `ESCAPE_ARC_TRACK_ALPHA`; a label (`label` role, on the label pill of §6) `THREAT_LABEL_GAP_PX` above the arc                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Two readings, from the record's `escape.phase` (computed once in `ownCellIndicatorsFor`, never re-derived by the renderer or the mirror). **`cover` / `wrap`:** the arc is the escape window, `fill = 1 − engulfProgress / ENGULF_SEAL_PROGRESS` (drains clockwise from full to empty at the seal: leaving contact still frees the prey, ecology/absorption.md §6.1), label `SPRINT TO ESCAPE`. **`absorb`** (from the seal on): the ring locks solid (the track at full alpha, no draining arc) and the label reads `SEALED`: only a spit-out or the ratio frees the prey now, so the player stops fighting a lost hold. The label appears and changes with no fade and no pulse (the whole window is about a second). The engulfing predator's own warning ring (`escape.predatorCellId`) is hidden while the arc shows, so two danger rings never run at the same radius; the other threats' rings stay. The predator's membrane shows the same progress (its arms and seal, rendering/contents-and-motion.md §4), so no other bar exists. A `cell_released` effect for the own cell (architecture/entity-model.md §2, reasons in ecology/absorption.md §6.1) drops the arc and the label at once, before the next snapshot clears `being_engulfed`. Hidden otherwise.                                                                                                                                                                                                                                                                                                                                                       |
| **Threat label**  | `threats` = `threatsFor({ cells, ownCell, cameraExtent, players, balance })`: cells for which `canEngulf(cell, ownCell, balance.absorption)` holds (the shared predicate of ecology/absorption.md §6.1, `shared/simulation/engulf-eligibility.ts`, the same call the server's engulf check and the renderer's warning ring make, so label, ring and engulf agree exactly, Cell Wall included) **wholly or partly inside `cameraExtent`** (§7) by `isDiscVisibleInExtent` — a plain containment test, **not** the renderer's `isDiscInExtent`, whose draw-cull margin of two radii admits a predator whose nearest edge is a full radius outside the frame; the label anchors to that predator's warning ring, and a ring that is not drawn is nothing to anchor to — nearest first; `players[threat.playerId].playerName` | on the **nearest** threat's warning ring (the renderer's, visual-style/motion-and-legibility.md §5), `THREAT_LABEL_GAP_PX` off the ring on the side that faces the own cell (so it is never off-screen and never under a corner panel); **when that pill would overlap the own cell's orbit extent (§3.1.3) it moves to the far side of the ring** (the predator is near the centre by then, so it stays on screen), so the label never crosses the counters or the numeral; `label` role, `WHITE`, on the label pill (§6); upright, never rotated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `AMOEBOID CAN ENGULF YOU` (the threat's name, uppercase by role); one label at a time, steady (no pulse); hidden while `being_engulfed` (the escape arc takes over) and when no threat is on screen. The rings on every threat stay the renderer's.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

**Mass** is the one fact the six do not carry. The size lock that made "the cell's size is the mass" untrue is gone
(Z1, game-design/controls-and-scope.md §7), and the number, its trend and its causes are the legibility cues of
§3.1.5 (the mass chip, rate tags and floaters), on the cell rather than in a corner; the full leaderboard and the
status mirror still carry it too. **Owned traits** are the organelles themselves
(visual-style/cells-and-organelles.md §4); their names, tiers and effects are listed in the hold-Tab panel
(overlays.md §3.7), in the menu (§3.5) and in the status mirror.

#### 3.1.3 The reading floor (the fact #146 solves)

Option C as drawn scaled everything with the cell and failed at the sizes the camera actually produces: under the
size lock the own cell is 24 px at spawn and 33 px for most of a round on the reference viewport, 32 / 45 px at
1080p, 102 px only at `CELL_MAX_MASS` (visual-style/motion-and-legibility.md §6). Under Z1 (decision #324,
game-design/controls-and-scope.md §7) it runs from 24 px at spawn through 47 px at mass 312 to 95 px at the cap on
the reference viewport, and 32 to 128 px at 1080p; the floors below are unchanged, and the second table gives the
geometry at those sizes. **Rule: every indicator has a size in screen px that never
goes below its floor, whatever `r_px` is**, the same idiom as the self ring's `SELF_RING_MIN_PX` and the warning
ring's `ENGULF_WARNING_RING_MIN_PX`. Sizes tied to the cell use `max(fraction × r_px, floor)`; the rest are fixed
px. Nothing here scales with `--hud-scale`, and nothing here falls back to chrome at any size. The constants are
§9's (home `render/constants.ts`, CODE-STANDARDS §2); rendering/own-cell-indicators.md §10 applies them.

Geometry at the sizes that matter (what `own-cell-geometry.spec.ts` pins, rendering/own-cell-indicators.md §10; px, angles in °):

| Own cell `r_px` | When                            | DNA ring ρ  | Keep-out (slot centres from) vs ring edge + pad | Self ring | Ladder orbit | One counter spans | Between the two backings | Orbit extent (backing edge) | Backing to seat-mark halo |
| --------------- | ------------------------------- | ----------- | ----------------------------------------------- | --------- | ------------ | ----------------- | ------------------------ | --------------------------- | ------------------------- |
| 24              | spawn on the reference viewport | 17 (0.71)   | 15.8 vs 20.0 (under; accepted, see below)       | 26.9      | 38.9         | 74                | 5                        | 46.9                        | 2.5                       |
| 32              | spawn at 1080p                  | 17 (0.53)   | 21.1 vs 20.0                                    | 35.8      | 47.8         | 60                | 21                       | 55.8                        | 3.4                       |
| 45              | most of a round at 1080p        | 19.8 (0.44) | 29.7 vs 22.8                                    | 50.4      | 62.4         | 46                | 37                       | 70.4                        | 4.5                       |
| 102             | `CELL_MAX_MASS` at 1080p        | 44.9 (0.44) | 67.3 vs 47.9                                    | 114.2     | 126.2        | 23                | 64                       | 134.2                       | 5.0                       |

**Under Z1 (decision #324).** The table above is the size-lock camera's, which `own-cell-geometry.spec.ts` pins
today. Z1 (game-design/controls-and-scope.md §7) lets the own cell grow on screen, so the sizes that matter
become the ones below; the camera ticket replaces the rows above with these (first cell as a bare number, as above)
and the spec's four sizes with these six in the same PR. Every value was computed by the shipped
`own-cell-geometry.ts` and `orbitLayout`, which reproduce the four rows above exactly; the sizes are
controls-and-scope.md §7's table. Nothing in the geometry changes except where the camera puts it: no floor moves.

| Own cell `r_px` (Z1) | When                                      | DNA ring ρ  | Keep-out vs ring edge + pad | Self ring | Ladder orbit | One counter spans | Between the two backings | Orbit extent | Backing to seat-mark halo |
| -------------------- | ----------------------------------------- | ----------- | --------------------------- | --------- | ------------ | ----------------- | ------------------------ | ------------ | ------------------------- |
| 24 px                | spawn on the reference viewport           | 17 (0.71)   | 15.8 vs 20.0 (under)        | 26.9      | 38.9         | 74                | 5                        | 46.9         | 2.5                       |
| 32 px                | spawn at 1080p                            | 17 (0.53)   | 21.1 vs 20.0                | 35.8      | 47.8         | 60                | 21                       | 55.8         | 3.4                       |
| 47.4 px              | mass 312 on the reference viewport        | 20.9 (0.44) | 31.3 vs 23.9                | 53.1      | 65.1         | 44                | 39                       | 73.1         | 4.5                       |
| 64 px                | mass 312 at 1080p                         | 28.2 (0.44) | 42.2 vs 31.2                | 71.7      | 83.7         | 34                | 50                       | 91.7         | 4.6                       |
| 94.8 px              | `CELL_MAX_MASS` on the reference viewport | 41.7 (0.44) | 62.6 vs 44.7                | 106.2     | 118.2        | 24                | 62                       | 126.2        | 4.9                       |
| 128 px               | `CELL_MAX_MASS` at 1080p                  | 56.3 (0.44) | 84.5 vs 59.3                | 143.4     | 155.4        | 18                | 69                       | 163.4        | 5.3                       |

Three inequalities the spec pins, at the four sizes and as a derivation:

- **Picker band.** Under the size lock the orbit's extent at the cap (134.2) stays under
  `HUD_PLAYER_EXCLUSION_PX + PICKER_BAND_GAP_PX` (136), so the picker band never touches it. **Under Z1 that
  ceiling breaks** (163.4 at 1080p, 211.2 at 2560 × 1440), and it is resolved by **moving the band, not by capping
  Z1's growth**: capping would give back the size lock above a mass, which is what decision #324 removed, and no
  single on-screen cap fits every viewport (the band scales with `--hud-scale`, the cell with the viewport's height).
  The band anchors at `pickerBandOffsetPx` (overlays.md §3.2) = `max((HUD_PLAYER_EXCLUSION_PX + PICKER_BAND_GAP_PX)
× s, capOrbitExtentPx + PICKER_BAND_ORBIT_CLEARANCE_PX)`, so the inequality holds by construction on every
  viewport; it moves nothing on the reference viewport (126.2 + 4 < 136) or at 1080p (163.4 + 4 < 183.6, the band
  scaled by 1.35). The camera ticket rewrites this check as that function's spec at 1024 × 640, 1280 × 800,
  1280 × 1000, 1920 × 1080 and 2560 × 1440.
- **Seat mark.** The backing's inner edge clears the seat mark's halo (visual-style/principles-and-palette.md §2: beads at 1.0 r, halo
  2.2 × `max(SEAT_MARK_BEAD_RADIUS_FRACTION × r_px, SEAT_MARK_BEAD_MIN_PX)`), for every seat index and every size:
  `ladderOrbitRadiusPx − LADDER_BACKING_PX / 2 ≥ r_px + haloPx + LADDER_SEAT_MARK_CLEARANCE_PX`. Below 40 px the
  halo is the 4.4 px floor and the margin is `0.12 r_px − 1.4` (≥ 0 from 12 px); above it the margin is
  `0.01 r_px + 3`. So the counters (which cover the lower half of the orbit at 24 px, where every index but 0 has a
  bead) never dim a bead; the size sheet's Mint tile shows a 4-bead player under them.
- **DNA keep-out.** Slot rest positions are cell-frame fractions computed once per cell (rendering/contents-and-motion.md §3), so the
  keep-out cannot be a screen-px floor: a px floor would move or hide organelles as the camera zooms. Instead the
  fraction is set from the floored ring. The inequality
  `DNA_RING_KEEP_OUT_FRACTION × r_px ≥ dnaRingRadiusPx + DNA_RING_STROKE_PX / 2 + DNA_RING_KEEP_OUT_PAD_PX`
  holds for every `r_px ≥ 31` (the floored edge is 20 px; `20 / 0.66 = 30.3`; above 39 px the edge is
  `0.44 r_px + 3`, under `0.66 r_px` from 14 px), which is every size but the reference viewport's
  24 px spawn. There, for the first seconds after a (re)spawn, the ring band overlaps the slot band and reads on
  its track, which is also its backing (§3.1.2); accepted, and the same window in which the band may reach the
  membrane below`CELL_LOD_FULL_MIN_PX` (a 19 px cell on the smallest supported viewport, 1024 × 640).

One layout case sits beside the table, and the spec pins it at the same four sizes. **A ghost beside a counter**
(decision #285 B): from `endosymbiosis` on, the rung ghost at 180 shares the orbit with an unclaimed counter at 225
or 135. The ghost keeps its angle. Clearance is measured between the boxes as drawn, each laid tangent to the orbit
(the ghosts' `LADDER_GHOST_PX` squares and the pip block), never along the arc: a tangent box's inner corners reach
past its arc angle. A counter whose nearer box would come closer to the rung ghost's box than
`LADDER_ITEM_CLEARANCE_PX` turns away (aerobic clockwise, photosynthetic counter-clockwise) by the smallest turn
that leaves exactly that gap: ghost to ghost for aerobic, pips to ghost for photosynthetic. That happens at 32 px
and below (the 24 and 32 px rows); from 34 px up both counters keep their §9 angles. Where the padded backings
meet, `orbitLayout` merges them into one band with no darker seam: at 24 and 32 px; they stand apart at 45 px.

#### 3.1.4 The `OwnCellIndicators` record and the status mirror

`GameStateService.ownCellIndicators` (§7) is a derived signal built by the pure `ownCellIndicatorsFor(...)`
(`state/own-cell-indicators.ts`, unit-tested with no DOM and no WebGL) from the signals above:

```ts
export const LADDER_SILHOUETTE = { nucleoid: 'nucleoid', envelope: 'envelope', form: 'form' } as const;
export type LadderSilhouette = ValueOf<typeof LADDER_SILHOUETTE>; // what the next rung draws, by stage
export interface OwnCellIndicators {
  level: number;
  dnaFraction: number; // 0..1; 1 at MAX_LEVEL
  isMaxLevel: boolean;
  // The next rung's ghost and the open counters are independent: both show once one endosymbiont is owned (#285 B).
  ladder: { ghost: { silhouette: LadderSilhouette; angleDeg: number } | null; counters: LadderCounter[] };
  sprintFill: number; // 0..1, 1 = ready
  isSprinting: boolean;
  escape: { progress: number; phase: EngulfPhase; fill: number; predatorCellId: EntityId } | null; // while being_engulfed
  nearestThreat: { cellId: EntityId; label: string } | null; // 'AMOEBOID CAN ENGULF YOU'
}
export interface LadderCounter {
  traitId: TraitId; // mitochondrion | chloroplast
  variant: BacteriumVariant;
  eaten: number; // clamped to `required`: the counter outlives the unlock, the pip row does not
  required: number;
  angleDeg: number; // from LADDER_ORBIT_ANGLES_PAIR_DEG
  isGhostHidden: boolean; // the picker previews this endosymbiont (§3.1.2)
  isUnlocked: boolean; // eaten >= required: the ghost takes its level-gold ring until the trait is picked
}
```

`escape.phase` is `engulfPhaseOf(engulfProgress, balance.absorption)` and `escape.fill` the window of §3.1.2, both
computed here once; the ladder's ghost hides by the same rule in `ladderFor` (§7). The level-up flash is not a
field: it is the renderer's `ringFlash` track on the `level_up` clip it already plays (rendering/contents-and-motion.md §4), so one clock
drives the burst and the ring.

Two consumers, one truth: the renderer draws it (the fourth HUD → Pixi crossing, §7), and the **status mirror**
speaks it. The mirror is one visually hidden element (`hud-own-cell`, `role="status"`, `aria-live="polite"`, the
`visually-hidden` clip pattern, never `display: none`) whose **attributes** update every snapshot and whose
**text** is rewritten only on an event, so assistive technology hears changes and never a 60 Hz stream:

| Attribute                             | Value                                                                |
| ------------------------------------- | -------------------------------------------------------------------- |
| `data-level`, `data-max-level`        | `4`, `true` / `false`                                                |
| `data-dna-percent`                    | `62` (floor)                                                         |
| `data-aerobic`, `data-photosynthetic` | `2/10`; absent when the counter is hidden                            |
| `data-ladder`                         | `ghost:nucleoid`, `ghost:envelope`, `ghost:form`, `counters`, `none` |
| `data-sprint`                         | `ready`, `cooling`, `sprinting`                                      |
| `data-engulfed`                       | `35` (engulf progress %) or absent                                   |
| `data-engulf-phase`                   | `cover`, `wrap`, `absorb` (`escape.phase`) or absent                 |
| `data-threat`                         | the nearest threat's `cellId` or absent                              |
| `data-traits`                         | `nucleoid:1 flagellum:2` (catalog order)                             |
| `data-mass`                           | `128`                                                                |

`data-ladder` names the ghost whenever one shows, so a cell with the envelope ghost and an unclaimed counter reads
`ghost:envelope` with that counter's `data-aerobic` or `data-photosynthetic` beside it; `counters` means counters
alone (the prokaryote), `none` neither.

Text (`body`, hidden): `Level 4 · DNA 62 % · Aerobic 2 of 10 · Photosynthetic 0 of 10 · Sprint ready`, rewritten
on a level change, on every `STATUS_ANNOUNCE_DNA_STEP_PERCENT` crossed, on a counter change, when a threat appears
(`Amoeboid can engulf you`), on `being_engulfed` (`Engulfed · sprint to escape`), at the seal (`Sealed`) and on
sprint ready. Playwright reads the attributes (§8); nothing in the mirror is interactive.

The legibility cues (§3.1.5) add four attributes, updated every snapshot like the rest:

| Attribute          | Value                                                                             |
| ------------------ | --------------------------------------------------------------------------------- |
| `data-mass-trend`  | `up`, `down`, `steady` (`massChip.trend`)                                         |
| `data-mass-rate`   | `-9.4` (`massChip.ratePerSecond`, one decimal, ASCII minus)                       |
| `data-mass-causes` | `toxin:-9.4 decay:-0.5 vent:-0.3` (the shown rate tags, in drawn order) or absent |
| `data-zone`        | a `ZoneId`: `warm_vent`, `sunlit_shallows`, `viscous_gel`, `open_broth`           |

The text gains `Shrinking 9 a second` / `Growing 3 a second` when the trend changes, and the zone's name on entry
(`Warm vent · decay times 1.5`). Floaters and relation rings are not mirrored: they are moment-to-moment, and the
facts behind them are the mass, the causes and the threat already here.

#### 3.1.5 Legibility cues (decision #324, option C)

The own-cell indicators say where the player is on the ladder. The cues say **what is happening to the cell right
now and why**: a mass chip with its trend, a rate tag per ongoing cause, a floater per one-off change, a zone pill
on entry, and a relation ring on every cell the player could eat or should not touch. They answer the audit's
missing rows (`qa/decisions/legibility/audit.md`, PR #325): own size, food gain, decay, vent decay, sprint cost,
gel, photosynthesis, toxin and spine drain, who can eat whom. The reference look is
`qa/decisions/legibility/legibility-c-coach-tab.png` on that PR; where the frame and this section differ (positions,
the toxic ring), this section wins.

**Rules shared by every cue.** Renderer-drawn in the own cell's undeformed frame, world-anchored and riding the
predicted position like §3.1.2's indicators; never DOM, never scaled by `--hud-scale`, inside the exclusion box by
design (layout.md §1). A number is `value`, its cause `label`, both `WHITE`; the colour is on the pill's
`CUE_RIM_PX` rim, never on text (visual-style/principles-and-palette.md §2's cue roles). A cue pill is
`CUE_PILL_HEIGHT_PX` tall (`value` needs it; the label pill of §6 stays 18 px for `label`-only text), full-height
radius, `CUE_PILL_PAD_PX` at each end, callout backing at `LABEL_PILL_ALPHA`. Every amount is the server's own
number (the `massFlow` facts below), formatted by `formatMassAmount` / `formatMassRate` (`hud/format/mass-cues.ts`:
`+3`, `−16`, `−0.5/s` under 10 with one decimal, `−12/s` from 10 up, U+2212 minus); none is re-derived from a
client-side formula.

**The facts the cues read (proposal; the wire change is the architect's, on the build ticket).** Today the snapshot
carries the own cell's mass but not why it moved: decay, the vent share, toxin reach and photosynthesis are
computed only in the server's `simulation/metabolism.ts`, and the `eat` effect carries no amount. Re-deriving them
on the client would be a second copy of the rules that drifts at the floors and the cap, so the server reports what
it applied:

```ts
export const MASS_RATE_CAUSE = {
  toxin: 'toxin',
  spines: 'spines',
  decay: 'decay',
  vent: 'vent',
  light: 'light',
} as const;
export type MassRateCause = ValueOf<typeof MASS_RATE_CAUSE>; // the declaration order is the tie-break order
export interface MassFlowView {
  /** mass/s the metabolism step applied to the own cell on this snapshot's tick; losses negative, 0 when off. */
  ratesPerSecond: Record<MassRateCause, number>; // decay is the broth share; vent the extra × (VENT_DECAY_MULTIPLIER − 1)
  /** The trait share inside decay: `decayMultiplier − 1` of the owned traits (−0.15 with Mitochondrion I); 0 with none. */
  decayTraitShare: number;
}
// OwnProgress gains `massFlow: MassFlowView | null` (null while spectating).
// EatEffect gains `massGained` and `dnaGained` (after modifiers and the cap; the cap's overflow is in `dnaGained`).
// CellAbsorbedEffect gains `predatorMassGained` and `predatorDnaGained`.
// The sprint cost moves into a new shared `sprintMassCost(mass, balance.controls, balance.growth)` (today it is inline
// in the server's `simulation/inputs.ts`: `mass × SPRINT_MASS_COST_FRACTION`, floored at CELL_STARTING_MASS); the
// server's sprint start calls it, so the client's `−16 SPRINT` is the same function, not a copy.
```

**The cues.** Positions are screen px in the cell frame (x right, y down, origin the cell centre), with
`selfRingPx` = §3.1.2's self-ring radius and `orbitExtentPx` = §3.1.3's orbit extent at the cell's current `r_px`.
All of it is laid out by one pure `cueLayout(record, rPx, orbitLayout)` (`render/effects/cue-layout.ts`), which
resolves every overlap by the rule in the Placement column, the way `orbitLayout` turns a counter away.

| Cue                | Source                                                                                                                                                                                                                                                                                                                                                   | Placement, size                                                                                                                                                                                                                                                                                                                                                                                                                     | States and text                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Mass chip**      | `ownCell.mass`; the trend from the own cell's mass over the last `MASS_TREND_WINDOW_SECONDS` of snapshots (`massTrendFor`, `state/mass-trend.ts`, pure)                                                                                                                                                                                                  | centred on x; bottom edge at y = −(`selfRingPx` + `CUE_GAP_PX`); a cue pill with no rim                                                                                                                                                                                                                                                                                                                                             | `312` (floor) then a trend glyph `TREND_GLYPH_PX` (a triangle: up in `GAIN`, down in `DANGER`; geometry, so the direction is not colour-only) and the net rate `9/s` (`value`, unsigned: the glyph carries the sign). `trend` is `up` / `down` when the net rate's size is ≥ `MASS_TREND_MIN_PER_SECOND`, else `steady`, which shows the mass alone. Always shown while alive: this replaces "mass has no indicator" (§3.1.2).                                                                                                                                                                                                                            |
| **Rate tags**      | `ownProgress.massFlow.ratesPerSecond`, `decayTraitShare`, `ownCell.traits` for the trait glyph                                                                                                                                                                                                                                                           | a column of cue pills centred on x, stacked upward from `CUE_ROW_GAP_PX` above the mass chip, `CUE_ROW_GAP_PX` apart; at most `RATE_TAG_ROWS_MAX`, the largest rate nearest the chip (ties in `MASS_RATE_CAUSE` order)                                                                                                                                                                                                              | One tag per cause whose rate's size is ≥ `RATE_TAG_MIN_MASS_PER_SECOND`: `−9.4/s TOXIN` (rim `DANGER`), `−2.1/s SPINES` (rim `DANGER`), `−0.5/s DECAY` (rim none; with a trait share, the owning trait's glyph in `TRAIT_CUE` and `−15 %` after the cause), `−0.3/s VENT` (rim `ZONE_CUE` of `warm_vent`), `+1/s LIGHT` (rim `ZONE_CUE` of `sunlit_shallows`). A tag's text refreshes at most every `RATE_TAG_REFRESH_MS`, so a decaying rate does not flicker its last digit; a tag appears and leaves with no fade.                                                                                                                                     |
| **Floaters**       | `eat` effects on the own cell (`massGained`, `dnaGained`), `cell_absorbed` with the own cell as predator, the sprint start (`sprintRemainingTicks` rising from 0: `sprintMassCost` of the mass before it), all on `renderTick`                                                                                                                           | left edge at x = `selfRingPx` + `CUE_GAP_PX`, bottom edge starting at y = −`CUE_GAP_PX`; rises `FLOATER_RISE_PX` over `FLOATER_LIFETIME_MS`, fading over its last `FLOATER_FADE_FRACTION`; a new floater starts at the bottom and pushes the ones still rising up by one row; if a floater's box would meet the mass chip's or a rate tag's box, `cueLayout` moves the whole floater column right until they are `CUE_GAP_PX` apart | `+3 FOOD` (rim `GAIN`), `+5 DNA` (rim `DNA`; from a fragment, an engulf, or food past `CELL_MAX_MASS`), `+60 ENGULF` (rim `GAIN`), `−16 SPRINT` (no rim). Same cause within `FLOATER_MERGE_MS` merges into the youngest (`+3` then `+3` reads `+6 FOOD`). At most `FLOATER_MAX_VISIBLE`: an older one leaves early. The floaters are the one cue that moves and fades, an exception to input-and-onboarding.md §6's label rule, because a one-off change must read as one.                                                                                                                                                                                |
| **Zone pill**      | `ownCellIndicators.zone` = the shared `zoneAt(ownCell, gelPatches, balance)`, moved from the server's `simulation/zones.ts` to `shared/simulation/zones.ts`, so the pill and the decay the server applies read one function                                                                                                                              | centred on x; top edge at y = `orbitExtentPx` + `CUE_GAP_PX`; the label pill of §6 with a `ZONE_PILL_DOT_PX` dot in `ZONE_CUE` before the text                                                                                                                                                                                                                                                                                      | Shown for `ZONE_PILL_SECONDS` on entry into `warm_vent`, `sunlit_shallows` or `viscous_gel`; a re-entry into the same zone within `ZONE_PILL_COOLDOWN_SECONDS` shows nothing (so a cell on a boundary does not flash). Text (`hud/format/zone-pill.ts`, every number from balance): `WARM VENT · DECAY ×1.5 · ORANGE RODS` (`VENT_DECAY_MULTIPLIER`), `SUNLIT SHALLOWS · LIGHT · GREEN RODS`, `VISCOUS GEL · YOUR SPEED ×0.6` (`gelSpeedFactor` at the own mass, one decimal). The rod colour follows `BACTERIUM_VARIANT_WEIGHTS_BY_ZONE`'s larger variant. Hidden while `offer !== null` (the picker band is below the cell) and while `being_engulfed`. |
| **Relation rings** | `relationsFor({ cells, ownCell, cameraExtent, balance })` (`hud/format/relations-for.ts`, pure, beside `threatsFor` and over the same `isDiscVisibleInExtent`): `edible` = `canEngulf(ownCell, cell)`; `toxic` = the cell's folded `toxinDrainFractionPerSecond > 0` (`foldModifiers` over `cell.traits`); a threat is `threatsFor`'s and keeps its ring | the ring: principles-and-palette.md §2's relation rings, radius `max(RELATION_RING_RADII × r, r + RELATION_RING_MIN_GAP_PX)` on screen, stroke `RELATION_RING_STROKE_PX`; the label: the label pill of §6 (rim `GAIN` or `DANGER`, `DANGER_LABEL_RIM_PX`), placed on its ring by `threatLabelPlacement`'s rule (the side facing the own cell, flipped past the orbit extent)                                                        | Per on-screen cell: a threat → the warning ring only (toxic or not); `edible` and toxic → the `DANGER` solid ring; toxic and neither → the `DANGER` solid ring; `edible` alone → the `GAIN` solid ring. **Labels, at most one per kind:** the nearest threat's (§3.1.2, unchanged); on the nearest toxic cell `EDIBLE · TOXIC` or `TOXIC`; on the nearest edible, non-toxic cell `EDIBLE`, only until the player's first engulf of the session (after that the green ring is learnt). No label on a cell that is being engulfed by the own cell (the prey is already known).                                                                              |

**Layout inequalities `cue-layout.spec.ts` pins**, at the §3.1.3 sizes and at `CELL_MAX_MASS` on 1024 × 640 (the
smallest viewport) and 1280 × 800:

- **Notices.** The top of a full rate-tag column (`RATE_TAG_ROWS_MAX` rows over the chip) stays below
  `HUD_NOTICE_STACK_BOTTOM_PX` (the connection banner and error rows, overlays.md §3.6). At the cap on 1024 × 640 the
  cell is 75.9 px, the self ring 85.0, and the column top is 85.0 + 6 + 28 + 3 × (4 + 28) = 215.0 px above the
  centre: y 105 against 96. On 1280 × 800: y 164.
- **Orbit.** No cue box meets an orbit box (`orbitLayout`'s ghosts and pip blocks): the chip and tags are above the
  self ring, the orbit's items below it (angles 135–225, turned at most toward 90 / 270 at 24 px), the floaters
  start above the 3 o'clock line and the zone pill below the orbit extent.
- **Picker band.** The zone pill is hidden while the band is up, so it never meets it.

#### 3.1.6 Legibility cue constants

The values this doc owns for §3.1.5. Home: `packages/client/src/app/game/render/constants/legibility-cues.ts` (the
renderer applies them), except the three rows marked **state**, whose home is
`packages/client/src/app/game/state/legibility-constants.ts` because `state/` derives the record from them. The
build ticket adds a `legibility-cues-ledger.spec.ts` that parses this table the way `constants-ledger.spec.ts`
parses §9 (names and values), so the table and the file cannot drift. `SCORE_ABSORPTION_BONUS`,
`VENT_DECAY_MULTIPLIER`, the bloom multipliers and the gel curve come from `game_state.balance` and are not here.

| Constant                                | Value | Unit     | Rationale                                                                                                                                            |
| --------------------------------------- | ----- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CUE_PILL_HEIGHT_PX`                    | 28    | px       | A `value` (20 px) number with 4 px above and below; the audit's frames drew it at this size.                                                         |
| `CUE_PILL_PAD_PX`                       | 12    | px       | Text to the pill's end, as the frames.                                                                                                               |
| `CUE_RIM_PX`                            | 2     | px       | The role colour's only carrier on a cue; 1 px (the danger label rim) disappears at a floater's speed.                                                |
| `CUE_GAP_PX`                            | 6     | px       | A cue to the self ring or the orbit extent; `THREAT_LABEL_GAP_PX`'s value, so labels and cues keep one rhythm.                                       |
| `CUE_ROW_GAP_PX`                        | 4     | px       | Between stacked cues.                                                                                                                                |
| `TREND_GLYPH_PX`                        | 10    | px       | The trend triangle's height: reads beside a 20 px numeral without competing with it.                                                                 |
| `RATE_TAG_ROWS_MAX`                     | 3     | rows     | The column stays under the notice stack on the smallest viewport at the cap (§3.1.5 inequality); decay, vent and toxin, the audit's worst case, fit. |
| `RATE_TAG_MIN_MASS_PER_SECOND`          | 0.1   | mass/s   | The smallest rate the one-decimal format shows as non-zero; decay reaches it at mass 70 in the broth.                                                |
| `RATE_TAG_REFRESH_MS`                   | 500   | ms       | Two text updates a second: a rate moving with the mass reads steady.                                                                                 |
| `FLOATER_RISE_PX`                       | 24    | px       | Under one cue row: the column reads as a stack, not a fountain.                                                                                      |
| `FLOATER_LIFETIME_MS`                   | 1200  | ms       | Long enough to read `+60 ENGULF` at a glance; short enough that eating a cluster stays at three or four.                                             |
| `FLOATER_FADE_FRACTION`                 | 0.3   | ×        | The last 30 % of the lifetime fades.                                                                                                                 |
| `FLOATER_MERGE_MS`                      | 300   | ms       | A mote cluster eaten in one pass is one floater.                                                                                                     |
| `FLOATER_MAX_VISIBLE`                   | 4     | floaters | The fourth push is where the column reaches the mass chip at the smallest cell.                                                                      |
| `ZONE_PILL_SECONDS`                     | 4     | s        | `HINT_DURATION_SECONDS`'s value: the zone pill is read once, like a hint.                                                                            |
| `ZONE_PILL_COOLDOWN_SECONDS`            | 20    | s        | A cell grazing a zone boundary is told once.                                                                                                         |
| `ZONE_PILL_DOT_PX`                      | 8     | px       | The zone dot, as the audit frame.                                                                                                                    |
| `RELATION_RING_RADII`                   | 1.3   | × r      | `ENGULF_WARNING_RING_RADII`'s value, so every relation ring sits at one distance.                                                                    |
| `RELATION_RING_MIN_GAP_PX`              | 6     | px       | Ring outside the membrane on a small far cell.                                                                                                       |
| `RELATION_RING_STROKE_PX`               | 1.5   | px       | Thinner than the 2 px dashed threat ring: solid-and-thin against dashed-and-glowing is the tell.                                                     |
| `MASS_TREND_WINDOW_SECONDS` (**state**) | 1     | s        | The net rate over a second: a single eat does not flip the trend, a sprint does.                                                                     |
| `MASS_TREND_MIN_PER_SECOND` (**state**) | 0.2   | mass/s   | Below this the chip reads steady; the broth decay of a mass-120 cell is the first to show.                                                           |
| `RELATIONS_MAX_RINGED` (**state**)      | 16    | cells    | Rings on the nearest 16 on-screen cells: the budget line (rendering/own-cell-indicators.md §10).                                                     |
