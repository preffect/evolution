# Evolution — UI: HUD, overlays and onboarding: in-round HUD and own-cell indicators

§3–§3.1 of the split [`UI.md`](../UI.md), which keeps the shared context and the file list.

## 3. In-round HUD and overlays

Every element: source, placement, size, states, text, test id. Text is short and literal (values, not sentences)
except hints and toasts. Numbers are integers; masses round down.

### 3.1 In-round elements (visible while `roundPhase === 'playing'` and `lifeState === 'alive'`)

Option C has two kinds of in-round element. **Chrome** (§3.1.1) is DOM in the corners: the leaderboard and the
round clock, nothing else. **Own-cell indicators** (§3.1.2) are drawn on and around the player's cell by the
renderer from one record the HUD derives (`OwnCellIndicators`, §3.1.4); the level ring, mass readout, DNA line,
ladder hint, minimap, trait strip, sprint meter, key hints and danger chip of the pre-#146 spec are gone, and
their facts live on the cell, in the full leaderboard (mass, §3.1.1), in the menu's trait list (§3.5) and in the
status mirror (§3.1.4) that tests and assistive technology read.

#### 3.1.1 Chrome

| Element     | Source                                                                                                                                                          | Placement, size                                                                                                        | States and text                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `data-testid`                                                                                                                     |
| ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- |
| Leaderboard | `snapshot.leaderboard`, names from `players[id].playerName`, swatches from `mp.avatarAssignments()`; the full list adds `LeaderboardRow.mass` and `absorptions` | top-right (16,16), 240 × (26 header + 16 label strip + 24 per row), max 5 rows compact; the full list adds a 24 footer | Columns rank, swatch 10 px, name (ellipsis at 12 chars), level, score in `body`. The swatch is the palette base with a rim-colour ring and `seatMarkBeadCount(avatarIndex)` beads (`hud/format/seat-mark.ts`, §7) = `SEAT_MARK_BEADS[avatarIndex]` = `avatarIndex + 1`, from the shared constant the renderer's seat mark reads (visual-style/principles-and-palette.md §2), so a player can match dish to board; the results table (§3.4) reuses the same swatch. Own row tinted (visual-style/ui-type.md §7) and always present: if outside the top 5 it replaces row 5; **it is where the level is always legible as text**. Header `LEADERBOARD` in `caption` with `HOLD TAB` right-aligned in `caption` muted (`TAB HELD` while Tab holds the full list open; `CLICK TO CLOSE` after the header opened it). A `caption` **label strip** (`LEADERBOARD_LABEL_ROW_HEIGHT_PX` 16) sits under the header on both panels (decision #324): `LV SCORE` on the compact panel, `LV SCORE MASS ENGULFS` on the full one. The #321 audit found the unlabelled score read as nothing and `EATEN` read as food, so the column "never moved"; it counts cells engulfed and is named for that. **Tab held** (or the header clicked) sets `openOverlay = 'leaderboard'` and expands to the full list: up to 8 rows, 360 wide, adds the mass and engulfs columns and a footer row (`LEADERBOARD_FOOTER_ROW_HEIGHT_PX`) in `label`, `Score = DNA + 25 per engulf · kept on death`, mixed case at `label` size (visual-style/ui-type.md §7), because the uppercase line overran the 344 px content box (25 = `balance.session.SCORE_ABSORPTION_BONUS`, formatted, never typed); the hold-Tab panel (overlays.md §3.7) opens with it. Rows re-sort with a 200 ms slide. The panel widens over `LEADERBOARD_EXPAND_MS`, and the full columns and footer arrive only when it has finished (`full-layout`, settled from the panel's running width transition itself — when it finishes, when it is cancelled, or at once when none runs — never from one `transitionend`, which a fast release and re-press or a hidden panel never fires): drawn from the first frame they were squeezed and clipped for the whole widening (#615). Closing drops them at once, before the panel narrows. Columns are **fixed** tracks, not `auto`, and the label strip is laid out from the same track list, so a label always sits over the column it names; each numeric track is wide enough for its `caption` label as well as its figures (`LEADERBOARD_SCORE_COLUMN_PX` 44, `LEADERBOARD_MASS_COLUMN_PX` 38, `LEADERBOARD_ENGULFS_COLUMN_PX` 58), and the name track takes what is left: at least `LEADERBOARD_NAME_COLUMN_MIN_PX` 105 on the full list at scale 1, the width of a wide 12-character name in `body` (`BigHungryAmo`). A name ellipsises at 12 characters or at the column width, whichever comes first; the compact track (94 px) is set by the 5-digit score and cuts wide names earlier. A score of `COMPACT_FROM` (100 000) or more is written compact, `123k` or `1.2M`, rounded down (`formatQuantity`'s `compact` presentation, #427), so it never spills into the mass column; the widest figure below 100 million is five characters. Rank, name, level and score are `body`; the three numeric columns are `figure` (visual-style/ui-type.md §7: `body`'s size in the mono face, so the digits are tabular at a 24-px row); the label strip is `caption`. **Phase and life:** the board is the one in-round element exempt from §3.1's `lifeState === 'alive'` half — a dead player watching their killer is exactly who wants the ranking — but it does stand down for `results`, where the results overlay (§3.4) claims the same corner. | `leaderboard`, `leaderboard-header`, `leaderboard-labels`, `leaderboard-row-<playerId>`, `leaderboard-full`, `leaderboard-footer` |
| Round timer | `snapshot.roundTimeLeftMs`, `mp.sessionConfig().roundDurationSeconds`, `balance.session.ROUND_BLOOM_START_FRACTION`                                             | bottom-right (16,16); `clock` role `07:42`, `ROUND` in `caption` under it                                              | Formatted `m:ss` floor. In bloom the clock turns level gold and the caption says the effect, `BLOOM · FOOD ×1.5 · DNA DROPS ×2`, in `label` (it carries a fact, so not `caption`), right-aligned under the clock; the multipliers are `balance.ecology.FOOD_BLOOM_SPAWN_MULTIPLIER` and `DNA_FRAGMENT_BLOOM_SPAWN_MULTIPLIER` through `roundClockStateFor`, never typed (decision #324). Last 10 s: pulses once per second. `results` phase: hidden (the results overlay shows the countdown).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `hud-round-clock`, `hud-round-phase`                                                                                              |

The chrome's phase gate lives in `hud.component.ts`, which is the only component that reads `roundPhase` for both.

There is no key-hint line: Tab is printed on the leaderboard header, Space is taught by the `sprint` onboarding
beat (§5; the pill reads `TAP sprint` on touch devices), Escape is not taught.

#### 3.1.2 Own-cell indicators (what is shown; rendering/own-cell-indicators.md §10 owns how)

All six live in the own cell's undeformed frame (they never bend with the membrane) and follow the cell's
on-screen radius `r_px` with the floors of §3.1.3; every constant named here is a row of §9. Angles are clockwise
from 12 o'clock. Colours are roles; timings are rendering/contents-and-motion.md §4 clips or §9 rates, never a bare number.

| Indicator         | Source (architecture/entity-model.md §2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Placement, size                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | States                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **DNA ring**      | `ownProgress.dnaTowardNextLevel`, `levelUpCost(level, balance.progression)` (shared `simulation/level-costs.ts`, PROGRESSION §2), `balance.progression.MAX_LEVEL`; the `level_up` effect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | the nucleus halo: a ring centred on the cell at radius `max(DNA_RING_RADIUS_FRACTION × r_px, DNA_RING_MIN_RADIUS_PX)`, stroke `DNA_RING_STROKE_PX`, always inside the body (organelle slot centres keep outside `DNA_RING_KEEP_OUT_FRACTION`, rendering/contents-and-motion.md §3); a **track** in the callout-backing colour at `DNA_RING_TRACK_ALPHA`, `DNA_RING_TRACK_PAD_PX` wider than the fill on each side, so it shows where 100 % is and backs the fill over whatever lies under it (dark on every palette: measured 1.5–2.1:1 against the brightest body, Mint, on the size sheet); the fill an arc from 12 o'clock clockwise in the DNA colour, round caps                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `dnaFraction = dnaTowardNextLevel / levelUpCost`, tweened over `INDICATOR_FILL_TWEEN_MS`. At `MAX_LEVEL` the ring is full in level gold. On `level_up` the ring flashes gold with the clip's `ringFlash` track (rendering/contents-and-motion.md §4: one clock, the renderer's `renderTick`, so the flash and the burst start on the same frame), then the new, near-empty fill shows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Level numeral** | `ownProgress.level`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | the ring's centre; `value` role, text colour `WHITE`, on a `LEVEL_NUMERAL_OUTLINE_PX` outline in the callout-backing colour at `LEVEL_NUMERAL_OUTLINE_ALPHA` so it reads over a lit nucleus; never scales                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `1` … `12`; the ring floor (§3.1.3) is chosen so two digits fit inside it. Flashes with the ring (`ringFlash`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Ladder orbit**  | `ownCell.stage`, `STAGE_GATE_TRAITS` (`constants/ladder.ts`), `ownProgress.bacteriaEatenByVariant`, the endosymbiont's own `unlockedBy.count` in `TRAIT_CATALOG` (the same number the server's draft gate reads, and its only copy on the wire: `balance.ladder` carries no count, #286), `ownCell.traits`, `previewTraitId` (§7)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | an arc band outside the identity ring: radius `selfRingPx + LADDER_ORBIT_GAP_PX` (`selfRingPx` = visual-style/principles-and-palette.md §2's `SELF_RING_RADIUS_FRACTION × r_px` with its `SELF_RING_MIN_PX` floor; the gap clears the seat mark's halo at every size, §3.1.3). Items lie along the arc on a callout-backing arc `LADDER_BACKING_PX` wide at `LADDER_BACKING_ALPHA`, extended `LADDER_BACKING_END_PAD_PX` past the first and last item (a px pad whose angle comes from the radius, so it is the same 4 px at every size). A **ghost** is the dashed silhouette of what the next rung draws, keyed by `LadderSilhouette` (§3.1.4: `nucleoid` loop, `envelope` circle with pores, `form` slipper outline) or, on a counter, by its organelle (mitochondrion bean with cristae, chloroplast lens with granules), `LADDER_GHOST_PX` long, tangent to the arc, in its organelle colour (rim colour for nucleus parts). A counter's **pips** are `ENDOSYMBIOSIS_BACTERIA_REQUIRED` pips in rows of `LADDER_PIP_ROW_MAX` (two rows of five at the current 10, counted the way a tally is), ⌀ `LADDER_PIP_PX`, gap `LADDER_PIP_GAP_PX` between pips and between rows, straight along the tangent `LADDER_ITEM_GAP_PX` after the ghost, the row nearest the cell first; one atlas entry per (variant, eaten), so a counter is two sprites (rendering/own-cell-indicators.md §10) | Per stage. `protocell` → ghost `nucleoid` at `LADDER_ORBIT_ANGLE_SINGLE_DEG` 180. `prokaryote` → two **counters** at `LADDER_ORBIT_ANGLES_PAIR_DEG` (aerobic 225, photosynthetic 135): ghost, then pips lit clockwise in the organelle colour at `LADDER_PIP_LIT_ALPHA` (unlit: a `LADDER_PIP_STROKE_PX` outline at `LADDER_PIP_UNLIT_ALPHA`) as `bacteriaEatenByVariant[variant]` grows; at `required` the ghost gains a level-gold ring `LADDER_UNLOCK_RING_PAD_PX` outside it, `LADDER_UNLOCK_RING_STROKE_PX` wide, until the trait is picked (this is the counter gameplay-qa flagged: without it the vent trip is a hidden requirement). An owned endosymbiont hides its counter; the other keeps its angle and stays on the orbit through `endosymbiosis`, `eukaryote` and `specialised`, beside that stage's ghost, until it is owned too (decision #285 B: the draft offers an endosymbiont from `prokaryote` on). Where a ghost shares the orbit, the ghost keeps its angle and the counter turns away from it (aerobic clockwise, photosynthetic counter-clockwise) just far enough that the drawn boxes stay `LADDER_ITEM_CLEARANCE_PX` apart (§3.1.3). `endosymbiosis` → ghost `envelope` at 180; `eukaryote` → ghost `form` at 180 (`STAGE_GATE_TRAITS.specialised` holds five forms, so the ghost is keyed by the next stage's silhouette, never by one trait); `specialised` → no ghost. While the picker (§3.2) previews a card whose trait is in `STAGE_GATE_TRAITS[nextStage]`, the ghost hides (on a counter, only the previewed endosymbiont's ghost; its pips stay): the preview shows the real organelle. |
| **Sprint ring**   | `ownCell.sprintCooldownRemainingTicks` (no client estimate), `ownCell.sprintRemainingTicks`, `balance.controls.SPRINT_COOLDOWN_SECONDS`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **the identity ring itself** (visual-style/principles-and-palette.md §2's self ring: its radius, dash, width, rotation and colour are unchanged)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Always drawn: the recharged part at the self ring's alpha and the remainder as a track at `SELF_RING_TRACK_ALPHA`, so the dashed circle (the own cell's non-colour tell, visual-style/principles-and-palette.md §2) is present at every fill, the frame after a sprint included. Ready (`sprintCooldownRemainingTicks === 0`): the full ring. Cooling: the ring is drawn as an arc from 12 o'clock to `sprintFill` = `1 − remaining / secondsToTicks(SPRINT_COOLDOWN_SECONDS)` clamped to [0, 1] (a cooldown shortened by a folded modifier starts partly drawn). Sprinting (`sprintRemainingTicks > 0`): full ring at the sprint rim brightness (visual-style/motion-and-legibility.md §5). Reaching ready: the `sprint_ready` clip (rendering/contents-and-motion.md §4), no sound of its own.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Escape arc**    | `ownCell.states` contains `being_engulfed`, `ownCell.engulfProgress`, `ownCell.engulfedByCellId`, `engulfPhaseOf(engulfProgress, balance.absorption)` (shared `simulation/engulf-pace.ts`, ecology/absorption.md §6.1, the function the doc names for "showing the phase")                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | replaces the ladder orbit on its radius while `being_engulfed`: a danger-colour arc, stroke `ESCAPE_ARC_STROKE_PX`, on a danger track at `ESCAPE_ARC_TRACK_ALPHA`; a label (`label` role, on the label pill of §6) `THREAT_LABEL_GAP_PX` above the arc                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Two readings, from the record's `escape.phase` (computed once in `ownCellIndicatorsFor`, never re-derived by the renderer or the mirror). **`cover` / `wrap`:** the arc is the escape window, `fill = 1 − engulfProgress / ENGULF_SEAL_PROGRESS` (drains clockwise from full to empty at the seal: leaving contact still frees the prey, ecology/absorption.md §6.1), label `SPRINT TO ESCAPE`. **`absorb`** (from the seal on): the ring locks solid (the track at full alpha, no draining arc) and the label reads `SEALED`: only a spit-out or the ratio frees the prey now, so the player stops fighting a lost hold. The label appears and changes with no fade and no pulse (the whole window is about a second). The engulfing predator's own warning ring (`escape.predatorCellId`) is hidden while the arc shows, so two danger rings never run at the same radius; the other threats' rings stay. The predator's membrane shows the same progress (its arms and seal, rendering/contents-and-motion.md §4), so no other bar exists. A `cell_released` effect for the own cell (architecture/entity-model.md §2, reasons in ecology/absorption.md §6.1) drops the arc and the label at once, before the next snapshot clears `being_engulfed`. Hidden otherwise.                                                                                                                                                                                                                                                                                                                                                       |
| **Threat label**  | `threats` = `threatsFor({ cells, ownCell, cameraExtent, players, balance })`: cells for which `canEngulf(cell, ownCell, balance.absorption)` holds (the shared predicate of ecology/absorption.md §6.1, `shared/simulation/engulf-eligibility.ts`, the same call the server's engulf check and the renderer's warning ring make, so label, ring and engulf agree exactly, Cell Wall included) **wholly or partly inside `cameraExtent`** (§7) by `isDiscVisibleInExtent` — a plain containment test, **not** the renderer's `isDiscInExtent`, whose draw-cull margin of two radii admits a predator whose nearest edge is a full radius outside the frame; the label anchors to that predator's warning ring, and a ring that is not drawn is nothing to anchor to — nearest first; `players[threat.playerId].playerName` | on the **nearest** threat's warning ring (the renderer's, visual-style/motion-and-legibility.md §5), `THREAT_LABEL_GAP_PX` off the ring on the side that faces the own cell (so it is never off-screen and never under a corner panel); **when that pill would overlap the own cell's orbit extent (§3.1.3) it moves to the far side of the ring** (the predator is near the centre by then, so it stays on screen), so the label never crosses the counters or the numeral; `label` role, `WHITE`, on the label pill (§6); upright, never rotated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `AMOEBOID CAN ENGULF YOU` (the threat's name, uppercase by role); one label at a time, steady (no pulse); hidden while `being_engulfed` (the escape arc takes over) and when no threat is on screen. The rings on every threat stay the renderer's.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

**Mass** is the one fact the six do not carry. The size lock that made "the cell's size is the mass" untrue is gone
(Z1, game-design/controls-and-scope.md §7), and the number, its trend and its causes are the legibility cues of
§3.1.5 (the mass chip, rate tags and floaters), on the cell rather than in a corner; the full leaderboard and the
status mirror still carry it too. **Owned traits** are the organelles themselves
(visual-style/cells-and-organelles.md §4); their names, tiers and effects are listed in the hold-Tab panel
(overlays.md §3.7), in the menu (§3.5) and in the status mirror.

#### 3.1.3 The reading floor (the fact #146 solves)

Option C as drawn scaled everything with the cell and failed at the sizes the camera actually produces. Under Z1
(decision #324, game-design/controls-and-scope.md §7) the own cell runs from 24 px at spawn through 47 px at mass 312
to 95 px at `CELL_MAX_MASS` on the reference viewport, and from 32 to 128 px at 1080p
(visual-style/motion-and-legibility.md §6; the size lock Z1 replaced held it at 33 px for most of a round and hid
growth and decay). **Rule: every indicator has a size in screen px that never
goes below its floor, whatever `r_px` is**, the same idiom as the self ring's `SELF_RING_MIN_PX` and the warning
ring's `ENGULF_WARNING_RING_MIN_PX`. Sizes tied to the cell use `max(fraction × r_px, floor)`; the rest are fixed
px. Nothing here scales with `--ui-scale`, and nothing here falls back to chrome at any size. The constants are
§9's (home `render/constants.ts`, CODE-STANDARDS §2); rendering/own-cell-indicators.md §10 applies them.

Geometry at the sizes that matter (what `own-cell-geometry.spec.ts` pins, rendering/own-cell-indicators.md §10; px,
angles in °). The sizes are controls-and-scope.md §7's table; Z1 moved none of the floors, only where the camera puts
the cell:

| Own cell `r_px` | When                                      | DNA ring ρ  | Keep-out (slot centres from) vs ring edge + pad | Self ring | Ladder orbit | One counter spans | Between the two backings | Orbit extent (backing edge) | Backing to seat-mark halo |
| --------------- | ----------------------------------------- | ----------- | ----------------------------------------------- | --------- | ------------ | ----------------- | ------------------------ | --------------------------- | ------------------------- |
| 24              | spawn on the reference viewport           | 17 (0.71)   | 15.8 vs 20.0 (under; accepted, see below)       | 26.9      | 38.9         | 74                | 5                        | 46.9                        | 2.5                       |
| 32              | spawn at 1080p                            | 17 (0.53)   | 21.1 vs 20.0                                    | 35.8      | 47.8         | 60                | 21                       | 55.8                        | 3.4                       |
| 47.4            | mass 312 on the reference viewport        | 20.9 (0.44) | 31.3 vs 23.9                                    | 53.1      | 65.1         | 44                | 39                       | 73.1                        | 4.5                       |
| 64              | mass 312 at 1080p                         | 28.2 (0.44) | 42.2 vs 31.2                                    | 71.7      | 83.7         | 34                | 50                       | 91.7                        | 4.6                       |
| 94.8            | `CELL_MAX_MASS` on the reference viewport | 41.7 (0.44) | 62.6 vs 44.7                                    | 106.2     | 118.2        | 24                | 62                       | 126.2                       | 4.9                       |
| 128             | `CELL_MAX_MASS` at 1080p                  | 56.3 (0.44) | 84.5 vs 59.3                                    | 143.4     | 155.4        | 18                | 69                       | 163.4                       | 5.3                       |

Three inequalities, at the six sizes and as a derivation:

- **Picker band.** The orbit's extent at the cap grows with the viewport's height (126.2 on the reference viewport,
  163.4 at 1080p, 211.2 at 2560 × 1440), so no fixed band offset clears it. It is resolved by **moving the band, not
  by capping Z1's growth**: capping would give back the size lock above a mass, which is what decision #324 removed,
  and no single on-screen cap fits every viewport (the band scales with `--ui-scale`, the cell with the viewport's
  height). The band anchors at `pickerBandOffsetPx` (overlays.md §3.2) = `max((HUD_PLAYER_EXCLUSION_PX +
PICKER_BAND_GAP_PX) × s, capOrbitExtentPx + PICKER_BAND_ORBIT_CLEARANCE_PX)`, so the inequality holds by
  construction on every viewport; it moves nothing on the reference viewport (126.2 + 4 < 136) or at 1080p
  (163.4 + 4 < 183.6, the band scaled by 1.35). `hud/format/picker-band.spec.ts` pins it at 1024 × 640, 1280 × 800,
  1280 × 1000, 1920 × 1080 and 2560 × 1440, and pins the two things decision #425's wider card must not move: the
  card row fits across each of those viewports, and the band still ends exactly on the reference viewport's bottom
  edge.
- **Seat mark.** The backing's inner edge clears the seat mark's halo (visual-style/principles-and-palette.md §2: beads at 1.0 r, halo
  2.2 × `max(SEAT_MARK_BEAD_RADIUS_FRACTION × r_px, SEAT_MARK_BEAD_MIN_PX)`), for every seat index and every size:
  `ladderOrbitRadiusPx − LADDER_BACKING_PX / 2 ≥ r_px + haloPx + LADDER_SEAT_MARK_CLEARANCE_PX`. Below 40 px the
  halo is the 4.4 px floor and the margin is `0.12 r_px − 1.4` (≥ 0 from 12 px); above it the margin is
  `0.01 r_px + 3`. So the counters (which cover the lower half of the orbit at 24 px, where every index but 0 has a
  bead) never dim a bead; the size sheet's Mint tile shows a 4-bead player under them.
- **DNA keep-out.** Slot rest positions are cell-frame fractions computed once per cell (rendering/contents-and-motion.md §3), so the
  keep-out cannot be a screen-px floor: a px floor would move or hide organelles as the camera zooms. Instead the
  fraction is set from the floored ring. The inequality
  `DNA_RING_KEEP_OUT_FRACTION × r_px ≥ dnaRingRadiusPx + DNA_RING_STROKE_PX / 2 + DNA_RING_KEEP_OUT_PAD_PX`
  holds for every `r_px ≥ 31` (the floored edge is 20 px; `20 / 0.66 = 30.3`; above 39 px the edge is
  `0.44 r_px + 3`, under `0.66 r_px` from 14 px), which is every size but the reference viewport's
  24 px spawn. There, for the first seconds after a (re)spawn, the ring band overlaps the slot band and reads on
  its track, which is also its backing (§3.1.2); accepted, and the same window in which the band may reach the
  membrane below`CELL_LOD_FULL_MIN_PX` (a 19 px cell on the smallest supported viewport, 1024 × 640).

One layout case sits beside the table, and the spec pins it at the same six sizes. **A ghost beside a counter**
(decision #285 B): from `endosymbiosis` on, the rung ghost at 180 shares the orbit with an unclaimed counter at 225
or 135. The ghost keeps its angle. Clearance is measured between the boxes as drawn, each laid tangent to the orbit
(the ghosts' `LADDER_GHOST_PX` squares and the pip block), never along the arc: a tangent box's inner corners reach
past its arc angle. A counter whose nearer box would come closer to the rung ghost's box than
`LADDER_ITEM_CLEARANCE_PX` turns away (aerobic clockwise, photosynthetic counter-clockwise) by the smallest turn
that leaves exactly that gap: ghost to ghost for aerobic, pips to ghost for photosynthetic. That happens at 32 px
and below (the 24 and 32 px rows); from 34 px up both counters keep their §9 angles. Where the padded backings
meet, `orbitLayout` merges them into one band with no darker seam: at 24 and 32 px; they stand apart at 47.4 px.

#### 3.1.4 The `OwnCellIndicators` record and the status mirror

`GameStateService.ownCellIndicators` (§7) is a derived signal built by the pure `ownCellIndicatorsFor(...)`
(`state/own-cell-indicators.ts`, unit-tested with no DOM and no WebGL) from the signals above:

```ts
export const LADDER_SILHOUETTE = { nucleoid: 'nucleoid', envelope: 'envelope', form: 'form' } as const;
export type LadderSilhouette = ValueOf<typeof LADDER_SILHOUETTE>; // what the next rung draws, by stage
export interface OwnCellIndicators {
  level: number;
  dnaFraction: number; // 0..1; 1 at MAX_LEVEL
  isMaxLevel: boolean;
  // The next rung's ghost and the open counters are independent: both show once one endosymbiont is owned (#285 B).
  ladder: { ghost: { silhouette: LadderSilhouette; angleDeg: number } | null; counters: LadderCounter[] };
  sprintFill: number; // 0..1, 1 = ready
  isSprinting: boolean;
  escape: { progress: number; phase: EngulfPhase; fill: number; predatorCellId: EntityId } | null; // while being_engulfed
  nearestThreat: { cellId: EntityId; label: string } | null; // 'AMOEBOID CAN ENGULF YOU'
}
export interface LadderCounter {
  traitId: TraitId; // mitochondrion | chloroplast
  variant: BacteriumVariant;
  eaten: number; // clamped to `required`: the counter outlives the unlock, the pip row does not
  required: number;
  angleDeg: number; // from LADDER_ORBIT_ANGLES_PAIR_DEG
  isGhostHidden: boolean; // the picker previews this endosymbiont (§3.1.2)
  isUnlocked: boolean; // eaten >= required: the ghost takes its level-gold ring until the trait is picked
}
```

`escape.phase` is `engulfPhaseOf(engulfProgress, balance.absorption)` and `escape.fill` the window of §3.1.2, both
computed here once; the ladder's ghost hides by the same rule in `ladderFor` (§7). The level-up flash is not a
field: it is the renderer's `ringFlash` track on the `level_up` clip it already plays (rendering/contents-and-motion.md §4), so one clock
drives the burst and the ring.

Two consumers, one truth: the renderer draws it (the fourth HUD → Pixi crossing, §7), and the **status mirror**
speaks it. The mirror is one visually hidden element (`hud-own-cell`, `role="status"`, `aria-live="polite"`, the
`visually-hidden` clip pattern, never `display: none`) whose **attributes** update every snapshot and whose
**text** is rewritten only on an event, so assistive technology hears changes and never a 60 Hz stream:

| Attribute                             | Value                                                                |
| ------------------------------------- | -------------------------------------------------------------------- |
| `data-level`, `data-max-level`        | `4`, `true` / `false`                                                |
| `data-dna-percent`                    | `62` (floor)                                                         |
| `data-aerobic`, `data-photosynthetic` | `2/10`; absent when the counter is hidden                            |
| `data-ladder`                         | `ghost:nucleoid`, `ghost:envelope`, `ghost:form`, `counters`, `none` |
| `data-sprint`                         | `ready`, `cooling`, `sprinting`                                      |
| `data-engulfed`                       | `35` (engulf progress %) or absent                                   |
| `data-engulf-phase`                   | `cover`, `wrap`, `absorb` (`escape.phase`) or absent                 |
| `data-threat`                         | the nearest threat's `cellId` or absent                              |
| `data-traits`                         | `nucleoid:1 flagellum:2` (catalog order)                             |
| `data-mass`                           | `128`                                                                |

`data-ladder` names the ghost whenever one shows, so a cell with the envelope ghost and an unclaimed counter reads
`ghost:envelope` with that counter's `data-aerobic` or `data-photosynthetic` beside it; `counters` means counters
alone (the prokaryote), `none` neither.

Text (`body`, hidden): `Level 4 · DNA 62 % · Aerobic 2 of 10 · Photosynthetic 0 of 10 · Sprint ready`, rewritten
on a level change, on every `STATUS_ANNOUNCE_DNA_STEP_PERCENT` crossed, on a counter change, when a threat appears
(`Amoeboid can engulf you`), on `being_engulfed` (`Engulfed · sprint to escape`), at the seal (`Sealed`) and on
sprint ready. Playwright reads the attributes (§8); nothing in the mirror is interactive.

The legibility cues (§3.1.5) add four attributes, updated every snapshot like the rest:

| Attribute          | Value                                                                             |
| ------------------ | --------------------------------------------------------------------------------- |
| `data-mass-trend`  | `up`, `down`, `steady` (`massChip.trend`)                                         |
| `data-mass-rate`   | `-9.4` (`massChip.ratePerSecond`, one decimal, ASCII minus)                       |
| `data-mass-causes` | `toxin:-9.4 decay:-0.5 vent:-0.2` (the shown rate tags, in drawn order) or absent |
| `data-zone`        | a `ZoneId`: `warm_vent`, `sunlit_shallows`, `viscous_gel`, `open_broth`           |

The text gains `Shrinking 9.4 a second` / `Growing 3 a second` (the chip's figure) when the trend changes, and the
zone pill's line while the pill is up (`Warm vent · decay times 1.5 · orange rods`). Floaters and relation rings are
not mirrored: they are moment-to-moment, and the facts behind them are the mass, the causes and the threat already
here.

The cues' half of the record is `LegibilityCues` (`state/legibility-cues.ts`), which `OwnCellIndicators` extends:
`massChip { mass, trend, ratePerSecond, rateText }` (mass floored), `rateTags: RateTag[]` in drawn order (the cause,
the server's rate, its text, the rim role and, on the DECAY tag, the trait share), `zone { zone, pillText }` (`null`
without `massFlow`; `pillText` is `null` while the pill is down, while `offer !== null` and while `being_engulfed`)
and `sprintSpent { amount, tick }`. The floaters are not a field: the renderer spawns them from the frame's own
`eat` and `cell_absorbed` effects and from `sprintSpent`, once per tick. The trend and the zone pill's entries are
questions about history, so `GameStateService` carries their memories from snapshot to snapshot in two
`linkedSignal`s (`massTrendFor`, `state/mass-trend.ts`; `zoneEntryFor`, `hud/format/zone-pill.ts`); the pill's
`ZONE_PILL_SECONDS` and `ZONE_PILL_COOLDOWN_SECONDS` are counted in snapshot ticks, never on a wall clock.

#### 3.1.5 Legibility cues (decision #324, option C)

The own-cell indicators say where the player is on the ladder. The cues say **what is happening to the cell right
now and why**: a mass chip with its trend, a rate tag per ongoing cause, a floater per one-off change, a zone pill
on entry, and a relation ring on every cell the player could eat or should not touch. They answer the audit's
missing rows (`qa/decisions/legibility/audit.md`, PR #325): own size, food gain, decay, vent decay, sprint cost,
gel, photosynthesis, toxin and spine drain, who can eat whom. The reference look is
`qa/decisions/legibility/legibility-c-coach-tab.png` on that PR; where the frame and this section differ (positions,
the toxic ring), this section wins.

**Rules shared by every cue.** Renderer-drawn in the own cell's undeformed frame, world-anchored and riding the
predicted position like §3.1.2's indicators; never DOM, never scaled by `--ui-scale`, inside the exclusion box by
design (layout.md §1). **While a trait offer is open (§3.2) the mass chip and the rate tags stay drawn, under the
picker's dim, and only the zone pill hides**: the dim is the picker's spotlight over the dish, and what the cell is
doing to its mass is exactly what the pick is weighed against. A number is `value`, its cause `label`, both `WHITE`; the colour is on the pill's
`CUE_RIM_PX` rim, never on text (visual-style/principles-and-palette.md §2's cue roles). A cue pill is
`CUE_PILL_HEIGHT_PX` tall (`value` needs it; the label pill of §6 stays 18 px for `label`-only text), full-height
radius, `CUE_PILL_PAD_PX` at each end, callout backing at `LABEL_PILL_ALPHA`. Every amount is the server's own
number (the `massFlow` facts below), formatted by `formatMassAmount` / `formatMassRate` (`hud/format/mass-cues.ts`:
`+3`, `−16`, `−0.5/s` under 10 with one decimal, `−12/s` from 10 up, U+2212 minus); none is re-derived from a
client-side formula.

**The facts the cues read (landed with #383; wire-contract.md §4 "Mass flow").** Before #383 the snapshot
carried the own cell's mass but not why it moved: decay, the vent share, toxin reach and photosynthesis are
computed only in the server's `simulation/metabolism.ts`, and the `eat` effect carried no amount. Re-deriving them
on the client would be a second copy of the rules that drifts at the floors and the cap, so the server reports what
it applied:

```ts
export const MASS_RATE_CAUSE = {
  toxin: 'toxin', // contact or aura: every other cell whose toxin reaches this one (mass-and-movement.md §4.1 drainFraction)
  swallowed: 'swallowed', // the #154 dose of the prey this cell is engulfing: its toxin × ENGULF_SWALLOWED_TOXIN_MULTIPLIER + its spikes
  decay: 'decay', // the broth share: (m − CELL_STARTING_MASS) × MASS_DECAY_RATE_PER_SECOND × decayMultiplier
  vent: 'vent', // the vent's extra: the trait-scaled broth share × (VENT_DECAY_MULTIPLIER − 1)
  light: 'light', // photosynthesis as applied, after the cap
} as const;
export type MassRateCause = ValueOf<typeof MASS_RATE_CAUSE>; // the declaration order is the tie-break order
export interface MassFlowView {
  /** mass/s this tick's metabolism APPLIED to the own cell (post-floor, post-cap); losses negative; a zero cause is
   *  omitted; SNAPSHOT_MASS_RATE_DECIMALS (2) places. */
  ratesPerSecond: Partial<Record<MassRateCause, number>>;
  /** Product of the owned traits' decayMultiplier − 1 (−0.15 with Mitochondrion I; −0.235 with Chloroplast I too);
   *  omitted when 0. The DECAY tag shows the glyph of the trait with the largest share. */
  decayTraitShare?: number;
  /** The zone the metabolism step used this tick: the zone pill, the zone beat and the VENT tag read this, so they agree. */
  zone: ZoneId;
  /** Mass a sprint start took in this broadcast window, as applied (clipped at CELL_STARTING_MASS); omitted when none. */
  sprintSpent?: number;
}
// OwnProgress gains `massFlow: MassFlowView | null` (null while spectating), written from a transient per-player
// record beside world.effects: never a CellRecord field, never hashed or replayed, so the state hash is unchanged.
// EatEffect gains `massGained` and `dnaGained`; CellAbsorbedEffect gains `predatorMassGained` and `predatorDnaGained`.
// All four are measured as deltas around gainMass / gainDna (cap overflow in the DNA), SNAPSHOT_MASS_DECIMALS (0.1).
```

**Applied, at the floor and the cap.** `loseMassToFloor` clips decay and every drain together at
`CELL_STARTING_MASS`, so a rate is never the formula's value but what was taken: `loss = massAtStart −
massAfterFloor` is split across the requested losses (toxin, swallowed, decay, vent) in proportion to their formula
values, and `light = massAfterGain − massAfterFloor` is measured after the cap. So the causes always add up to the
mass actually lost or gained: `Δmass = Σ ratesPerSecond × TICK_INTERVAL_S + Σ own massGained + predatorMassGained −
sprintSpent` on every tick (#383's conservation scenario). A mass-20.3 cell touching Toxin Vacuole I shows the 0.3 it
lost, not the 0.6 the formula asks for. Light that overflows the cap becomes DNA (#179) and shows only in the DNA
ring; at the default balance it cannot happen (light plateaus at 186.67 / 395 / 662.86 mass, far under the cap).

**The cues.** Positions are screen px in the cell frame (x right, y down, origin the cell centre), with
`selfRingPx` = §3.1.2's self-ring radius and `orbitExtentPx` = §3.1.3's orbit extent at the cell's current `r_px`.
All of it is laid out by one pure `cueLayout(record, rPx, orbitLayout)` (`render/effects/cue-layout.ts`), which
resolves every overlap by the rule in the Placement column, the way `orbitLayout` turns a counter away.

| Cue                | Source                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Placement, size                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | States and text                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Mass chip**      | `ownCell.mass`; the net rate = Σ `massFlow.ratesPerSecond` + the own `massGained` / `predatorMassGained` / `−sprintSpent` of the last `MASS_TREND_WINDOW_SECONDS` as a rate (`massTrendFor`, `state/mass-trend.ts`, pure): the server's numbers, never a difference of 0.1-quantised masses. The window's history resets on a new own cell id, so a respawn never reads 312 → 20 as a fall                                                                                                                                                                                                        | centred on x; bottom edge at y = −(`selfRingPx` + `CUE_GAP_PX`), or `CUE_GAP_PX` above a label box it would meet there (§3.1.5 Labels); a cue pill with no rim                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `312` (floor) then a trend glyph `TREND_GLYPH_PX` (a triangle: up in `GAIN`, down in `DANGER`; geometry, so the direction is not colour-only) and the net rate `9/s` (`value`, unsigned: the glyph carries the sign). **Hysteresis:** `trend` turns `up` / `down` when the net rate's size reaches `MASS_TREND_ENTER_PER_SECOND` and returns to `steady` only when it falls under `MASS_TREND_EXIT_PER_SECOND`, so an idle cell whose decay sits on the threshold does not flicker (nor restart the `shrink` beat's hold). `steady` shows the mass alone. Always shown while alive: this replaces "mass has no indicator" (§3.1.2).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Rate tags**      | `ownProgress.massFlow.ratesPerSecond`, `decayTraitShare`, `ownCell.traits` for the trait glyph                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | a column of cue pills centred on x, stacked upward from `CUE_ROW_GAP_PX` above the mass chip, `CUE_ROW_GAP_PX` apart; at most `RATE_TAG_ROWS_MAX`, the largest rate nearest the chip (ties in `MASS_RATE_CAUSE` order)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | One tag per cause whose rate's size is ≥ `RATE_TAG_MIN_MASS_PER_SECOND`. At the worked example (mass 312 in the vent, Mitochondrion I, touching a Toxin Vacuole I cell; every value from the formulas above, which the tests compute from balance, never copy from here): `−9.4/s TOXIN` (rim `DANGER`; 312 × 0.03), `−0.5/s DECAY` (no rim; the glyph of the trait with the largest share in `TRAIT_CUE` and `×0.85` after the cause (the decay factor, below); the glyph is that endosymbiont's ghost entry of the indicator atlas, already baked in its organelle colour, since Mitochondrion and Chloroplast are the only traits with a `decayMultiplier`; 292 × 0.002 × 0.85 = 0.496), `−0.3/s VENT` (rim `ZONE_CUE` of `warm_vent`; 0.496 × 0.5 = 0.248, which the wire carries at `SNAPSHOT_MASS_RATE_DECIMALS` as 0.25 and the one-decimal format rounds to 0.3: a tag shows the number the server sent, never the unrounded formula). Elsewhere: `−23/s SWALLOWED` (rim `DANGER`; the dose of the prey being engulfed, toxin and spikes together: swallowing that Toxin Vacuole I cell at mass 96 is 96 × 0.03 × `ENGULF_SWALLOWED_TOXIN_MULTIPLIER` 8 = 23.04), `+0.3/s LIGHT` (rim `ZONE_CUE` of `sunlit_shallows`; Chloroplast I). A tag's text refreshes at most every `RATE_TAG_REFRESH_MS`, so a decaying rate does not flicker its last digit; a tag appears and leaves with no fade. |
| **Floaters**       | `eat` effects on the own cell (`massGained`, `dnaGained`), `cell_absorbed` with the own cell as predator (`predatorMassGained`, `predatorDnaGained`), `massFlow.sprintSpent`, all on `renderTick`; every amount the server's                                                                                                                                                                                                                                                                                                                                                                      | a floater's x is fixed when it spawns: left edge at `selfRingPx` + `CUE_GAP_PX`, pushed right past the chip's and the rate tags' boxes as they are at that moment until `CUE_GAP_PX` apart; a live floater then moves only on y (a tag appearing later never snaps it sideways, and neither does a merge). Bottom edge starts at y = −`CUE_GAP_PX`; it rises `FLOATER_RISE_PX` over `FLOATER_LIFETIME_MS`, fading over its last `FLOATER_FADE_FRACTION`; a new floater pushes the ones still rising up by one row                                                                                                                                                                                                                      | `+3 FOOD` (rim `GAIN`), `+5 DNA` (rim `DNA`; from a fragment, an engulf, or food past `CELL_MAX_MASS`), `+60 ENGULF` (rim `GAIN`), `−16 SPRINT` (no rim; none when `sprintSpent` is absent, so a cell at the floor shows no `−0`). **One live floater per cause** (#443, the rules below): a pickup of a cause already on screen adds to that floater (`+3` then `+3` reads `+6 FOOD`), so the column is at most `FLOATER_MAX_VISIBLE` rows — one per `FLOATER_CAUSE` — with no two rows ever naming the same cause. The floaters are the one cue that moves and fades, an exception to input-and-onboarding.md §6's label rule, because a one-off change must read as one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Zone pill**      | `ownCellIndicators.zone` = `ownProgress.massFlow.zone`, the zone the server's metabolism used this tick, so the pill, the zone beat and the `VENT` tag agree exactly (the predicted position could disagree for a few ticks on a boundary)                                                                                                                                                                                                                                                                                                                                                        | centred on x; top edge at y = `orbitExtentPx` + `CUE_GAP_PX`; the label pill of §6 with a `ZONE_PILL_DOT_PX` dot in `ZONE_CUE` before the text                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Shown for `ZONE_PILL_SECONDS` on entry into `warm_vent`, `sunlit_shallows` or `viscous_gel`; the first alive snapshot of a (re)spawn inside a zone **counts as an entry** (deterministic: a respawn in the vent is told it is in the vent). A re-entry into the same zone within `ZONE_PILL_COOLDOWN_SECONDS` shows nothing (so a cell on a boundary does not flash). Text (`hud/format/zone-pill.ts`, every number from balance): `WARM VENT · DECAY ×1.5 · ORANGE RODS` (`VENT_DECAY_MULTIPLIER`), `SUNLIT SHALLOWS · LIGHT · GREEN RODS`, `VISCOUS GEL · YOUR SPEED ×0.6` (`gelSpeedFactor` at the own mass, one decimal). The rod colour follows `BACTERIUM_VARIANT_WEIGHTS_BY_ZONE`'s larger variant. Hidden while `offer !== null` (the picker band is below the cell), while `being_engulfed`, and while any relation or threat label box meets its box: the zone is the least urgent fact, so it yields (it resumes if its time is not up).                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Relation rings** | `relationsFor({ cells, ownCell, cameraExtent, balance })` (`hud/format/relations-for.ts`, pure, beside `threatsFor` and over the same `isDiscVisibleInExtent`): `edible` = `canEngulf(ownCell, cell)` **and** `cell.engulfedByCellId === null` (not held by another predator) **and** `ownCell.engulfingCellId === null` (no cell is edible while the own cell is already engulfing; the spit-out refractory is server-only and not reflected); `toxic` = the cell's folded `toxinDrainFractionPerSecond > 0` (`foldModifiers` over `cell.traits`); a threat is `threatsFor`'s and keeps its ring | the ring: principles-and-palette.md §2's relation rings (edible a single line, toxic a double line), ring radius (the edible line and the toxic ring's inner line) `max(RELATION_RING_RADII × r, r + RELATION_RING_MIN_GAP_PX)` on screen, the toxic outer line `TOXIC_RING_LINE_GAP_PX` outside it, stroke `RELATION_RING_STROKE_PX`, alpha by role (`EDIBLE_RING_ALPHA`, `TOXIC_RING_ALPHA`); the label: the label pill of §6 (rim `GAIN` or `DANGER`, `DANGER_LABEL_RIM_PX`), reaching out from its ring on the first clear axis side, most-facing the own cell first (clear of other rings by `NEIGHBOUR_CLEARANCE_PX`, of placed labels and of the orbit extent), or dropped for the frame (rendering/own-cell-indicators.md §10) | Per on-screen cell (at most `RELATIONS_MAX_RINGED`, nearest first): a threat → the warning ring only (toxic or not); toxic (edible or not) → the `DANGER` double line; `edible` alone → the `GAIN` single line. **Labels, at most one per kind:** the nearest threat's (§3.1.2, unchanged); on the nearest toxic cell `EDIBLE · TOXIC` or `TOXIC`; on the nearest edible, non-toxic cell `EDIBLE`, only until the player's first engulf of the session (after that the single green line is learnt). No label on a cell that is being engulfed by the own cell (the prey is already known). **Spiny prey (PR #389 ruling, #539):** spiny is the folded `spikeDrainFractionPerSecond > 0`; a spiny prey keeps the edible ring (no third ring style) and the fact rides the label: `EDIBLE · SPINY`, or `EDIBLE · SPINY · TOXIC` on a cell that is also toxic (a spine on a cell the own cell cannot eat is not said). The ring cannot carry the spine, so after the first engulf the edible label is kept for spiny prey only, on the nearest one. The first engulf is the first `cell_absorbed` naming the own cell as predator; it holds through death and respawn and lasts the page session (`GameStateService` is app-wide; a reload resets it, a new room does not) (`hasEngulfedIn`, `hud/format/relation-labels.ts`).                                                                          |

**One live floater per cause, and what a merge does not touch (#443).** Through #385 every pickup spawned its own
floater and only a repeat within 300 ms merged, so grazing a mote cluster put three or four near-identical `+1 FOOD`
pills side by side — the clutter the legibility work exists to remove. A pickup now adds to the floater of its cause
for as long as that floater is on screen. Two rules decide the rest, and they are **decided, not incidental**:

- **A merge does not restart the clock.** The floater keeps the lifetime it was born with and leaves on that
  schedule, so a fast eater sees one counter climb and go, never a permanent running total. The mechanical reason is
  that a floater's rise and its fade are both functions of its age: restarting the clock would slide the pill back
  down toward the cell and re-brighten it on every bite, a visible bob at eating speed, and the only way to reset the
  fade without that would be to decouple the rise from the lifetime — a hovering label, which is not what §3.1.5
  wants a floater to be. The design reason is that the running total is the **mass chip's** job (`312 ▲ 9/s`); a
  floater reports a moment. The cost, accepted: a pickup landing inside the last `FLOATER_FADE_FRACTION` raises a
  number the player may not read at that alpha. Nothing is lost by it — the mass chip carries every gain — and the
  alternative, starting a second pill beside a fading one, is the duplicate this ticket removes.
- **A merge does not move the pill.** The floater keeps the `leftPx` its first pickup was given. There is no "follow
  the newest pickup" to choose: a floater is never drawn at the eaten thing, it is anchored to the own cell's 3
  o'clock line, so the only thing a merge could move it to is a freshly recomputed column start (the cell has grown,
  a rate tag has appeared). Moving it there is exactly the sideways snap the Placement column forbids.

Since a cause is on screen at most once, the column holds at most one row per `FLOATER_CAUSE` — four, which is
`FLOATER_MAX_VISIBLE`. That bound is now structural rather than a trim in `FloaterStack`, and
`floater-stack.spec.ts` pins it: adding a fifth cause without raising the constant turns that test red, where the old
trim would have silently dropped a floater.

**The clock rule keeps the column whole, not just still.** Every floater has the same lifetime and a `bornMs` no
merge moves, so they retire strictly oldest first — and rows are handed out oldest-highest, so the row a retiring
floater leaves is always the top one. The live rows are therefore always exactly `0…n−1`, with no gap. Restarting
the clock on a merge would break that: a middle floater fed by a late pickup would outlive the one above it and
leave a hole in the column where a pill used to be. That is a structural reason for the same rule the motion
argument above gives, and it is the one that would still hold if the rise were ever decoupled from the lifetime.

**The decay factor (#445).** The DECAY tag's trait share and the hold-Tab panel's decay row (overlays.md §3.7)
are one string from one function, `decayTraitShareOf` (`hud/format/mass-cues.ts`), and that string is the **factor**
the trait applies — `×0.85` — not the signed change `−15 %` it was through #385 and #387.

The reason is that both surfaces read it beside the loss it is not: `−0.5/s DECAY ×0.85` on the tag, `Decay ·
Mitochondrion ×0.85 · −0.5/s` on the row. A `−15 %` there puts two minus signs on one line pointing opposite ways —
the rate is a cost, the share is a saving — and a player reads the second as another cost. A factor has no sign to
misread, and it is the form the HUD already states every other decay modifier in. `leadingMultiplier`
(`hud/format/round-clock.ts`) is that form's one home.

**The contrast pair is the panel's, not the tag's, and the doc should not claim otherwise.** On the panel the
argument is visible: `Decay · Mitochondrion ×0.85` and `Vent · decay ×1.5` are adjacent permanent rows on the same
quantity, one below one and one above it, so a player who has learnt `×1.5 = worse` has the pair in their eyeline.
In the world there is no pair: the VENT rate tag directly above the DECAY tag carries no multiplier at all
(`−0.2/s VENT`), and the zone pill's `WARM VENT · DECAY ×1.5` is up only for `ZONE_PILL_SECONDS` on entering the
zone. So for nearly all the time the tag is up, `×0.85` stands alone with nothing on screen to calibrate it. That
is still the better read than `−15 %` beside `−0.4/s` — a factor asks the player to know which side of one they are
on, the old string asked them to see that two identical minus signs meant opposite things — but it is a weaker
support in the world than on the panel, and the choice rests on the panel.

The factor is **left out entirely unless it is a cut** (the wire's `decayTraitShare` is the folded
`decayMultiplier − 1`, so a cut is negative): with no cut there is nothing to name, and putting a factor above one
beside the glyph of the trait that cuts decay would credit that trait with the increase, so the tag and the row
drop it the way overlays.md §3.7 drops a zero row.

**A modifier's tone is its effect, never its sign (#453, decided).** The panel's `TRAITS` row reads
`Mitochondrion I · −15 % mass decay`, from the trait-modifier label table (`quantities/modifier-labels.ts`) the trait
cards and the encyclopedia share, where every effect is `<signed change> <noun>` (`+15 % speed`). The copy stays: the
sign is the number's true direction, and the noun says what it is the direction of. What changed is the **tone**. Each
label-table row names the trait data's own direction (`betterWhen`: `higher` or `lower`), and `modifierEffect` calls a
value a `benefit` or a `drawback` from it. Every surface that shows a modifier then puts the kit's **effect mark**
(`ui-effect-mark`, components-and-constants.md §10.2) before the value: a `UI_EFFECT_MARK_PX` triangle pointing **up** in
`GAIN` for a benefit, **down** in `DANGER` for a drawback. The value's text stays in the text colour, since a cue role
colours glyphs and never text (visual-style/principles-and-palette.md §2), and the triangle's direction carries the effect
on its own: under deuteranopia the two roles are two similar yellows. So `▲ −15 % mass decay` reads as the benefit it is
beside `Speed · −47 %`, and the column no longer asks the eye to tell a good minus from a bad one. The surfaces are the trait card's effect
lines, the menu's `Your traits` lines, this panel's `TRAITS` value and the encyclopedia's Effects by tier cells. This is
the consistent rule the human's #445 ruling asked for: one direction per modifier, one tone per effect, the same on all
four surfaces.

**One wording, both surfaces — measured, not assumed.** `saves 15 %` was the other candidate and it does **not**
fit. All figures at 1024 × 640 with `--ui-scale` at the `UI_SCALE_MIN` floor of 0.8, measured in the live table;
the panel's scroll viewport holds 260 px of row.

The decay row's name column overflows somewhere between **189.23 px** (the widest text still fitting) and
**192.52 px** (the narrowest that does not), bisected a character at a time — so read the capacity as **~189 px**,
never as the width of whatever string happens to ship. Against that threshold: `Decay · Mitochondrion −15 %` gives
a 185.11 px name cell, about two characters clear of overflow; `Decay · Mitochondrion ×0.85` gives 184.11 px, so
this change buys about a pixel, not a reprieve; `Decay · Mitochondrion saves 15 %` needs 193.27 px, past the
threshold, and it pushed the row 2.05 px outside the panel and gave the panel a horizontal scroll axis
(`scrollWidth` 262 against `clientWidth` 260). The two-organelle worst case `×0.77` **ties** `×0.85` exactly at a
163.55 px text and a 184.11 px cell — the digits are equal-advance — so it is no worse, not better.

A fitting row's right edge is always flush with the viewport's (287.0 px at this viewport, for `−15 %` and `×0.85`
alike), because the name column takes the row's slack. **A flush right edge is therefore not a tightness signal and
must not be measured as one**; the name cell against the ~189 px threshold is the only reading that means anything
here. So no surface needs a compact form of its own and no divergence is recorded.

**Layout inequalities `cue-layout.spec.ts` pins**, at the §3.1.3 sizes and at `CELL_MAX_MASS` on 1024 × 640 (the
smallest viewport) and 1280 × 800:

- **Notices.** The top of a full rate-tag column (`RATE_TAG_ROWS_MAX` rows over the chip) stays below
  `NOTICE_STACK_MAX_Y_PX` (the connection banner and error rows, overlays.md §3.6). At the cap on 1024 × 640 the
  cell is 75.9 px, the self ring 85.0, and the column top is 85.0 + 6 + 28 + 3 × (4 + 28) = 215.0 px above the
  centre: y 105 against 96. On 1280 × 800: y 164.
- **Orbit.** No cue box meets an orbit box (`orbitLayout`'s ghosts and pip blocks): the chip and tags are above the
  self ring, the orbit's items below it (angles 135–225, turned at most toward 90 / 270 at 24 px), the floaters
  start above the 3 o'clock line and the zone pill below the orbit extent.
- **Picker band.** The zone pill is hidden while the band is up, so it never meets it.
- **Labels.** No relation or threat label box (placed by `threatLabelPlacement`) meets a cue box, and the order of
  yielding is fixed: labels are placed first and never move for a cue; the **mass chip** rises past any label box it
  meets until its bottom edge is `CUE_GAP_PX` above that box, the rate tags stacked on it as usual (#512: the escape
  label sits `THREAT_LABEL_GAP_PX` over the escape arc, closer to the self ring than a cue pill is tall, so while
  `being_engulfed` the chip always rides above `SPRINT TO ESCAPE`); the **zone pill** hides while any label box
  meets it (the zone is the least urgent fact); a floater spawning where a label box sits starts its column past
  that box, as it does past a tag. Otherwise the chip and the rate tags sit inside the self ring's upper band, where
  a label flipped past the orbit extent does not reach. The spec pins #324's caveat scene: mass 312 at 1280 × 800, a Toxin
  Vacuole I cell touching at 135° with its `EDIBLE · TOXIC` label flipped outward (about y +88), a threat lower right
  with its label facing the own cell, and the zone pill (top at `orbitExtentPx` + `CUE_GAP_PX` = 79.1): the zone pill
  is hidden and both labels read. It pins the floaters against a threat label on the right the same way.

#### 3.1.6 Legibility cue constants

The values this doc owns for §3.1.5. Home: `packages/client/src/app/game/render/constants/legibility-cues.ts` (the
renderer applies them), except the three rows marked **state**, whose home is
`packages/client/src/app/game/state/legibility-constants.ts` because `state/` derives the record from them. The
build ticket adds a `legibility-cues-ledger.spec.ts` that parses this table the way `constants-ledger.spec.ts`
parses §9 (names and values), so the table and the file cannot drift. `SCORE_ABSORPTION_BONUS`,
`VENT_DECAY_MULTIPLIER`, the bloom multipliers and the gel curve come from `game_state.balance` and are not here.

| Constant                                  | Value | Unit     | Rationale                                                                                                                                                                                                                                                                      |
| ----------------------------------------- | ----- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `CUE_PILL_HEIGHT_PX`                      | 28    | px       | A `value` (20 px) number with 4 px above and below; the audit's frames drew it at this size.                                                                                                                                                                                   |
| `CUE_PILL_PAD_PX`                         | 12    | px       | Text to the pill's end, as the frames.                                                                                                                                                                                                                                         |
| `CUE_RIM_PX`                              | 2     | px       | The role colour's only carrier on a cue; 1 px (the danger label rim) disappears at a floater's speed.                                                                                                                                                                          |
| `CUE_GAP_PX`                              | 6     | px       | A cue to the self ring or the orbit extent; `THREAT_LABEL_GAP_PX`'s value, so labels and cues keep one rhythm.                                                                                                                                                                 |
| `CUE_ROW_GAP_PX`                          | 4     | px       | Between stacked cues.                                                                                                                                                                                                                                                          |
| `CUE_SEGMENT_GAP_PX`                      | 6     | px       | Between the parts inside one pill (the number, a glyph, the cause, a trait share): about a space in the `value` face, so `−9.4/s TOXIN` reads as a number and its cause.                                                                                                       |
| `TREND_GLYPH_PX`                          | 10    | px       | The trend triangle's height: reads beside a 20 px numeral without competing with it.                                                                                                                                                                                           |
| `RATE_TAG_ROWS_MAX`                       | 3     | rows     | The column stays under the notice stack on the smallest viewport at the cap (§3.1.5 inequality); decay, vent and toxin, the audit's worst case, fit.                                                                                                                           |
| `RATE_TAG_MIN_MASS_PER_SECOND`            | 0.1   | mass/s   | The smallest rate the one-decimal format shows as non-zero; decay reaches it at mass 70 in the broth.                                                                                                                                                                          |
| `RATE_TAG_REFRESH_MS`                     | 500   | ms       | Two text updates a second: a rate moving with the mass reads steady.                                                                                                                                                                                                           |
| `FLOATER_RISE_PX`                         | 24    | px       | Under one cue row: the column reads as a stack, not a fountain.                                                                                                                                                                                                                |
| `FLOATER_LIFETIME_MS`                     | 1200  | ms       | Long enough to read `+60 ENGULF` at a glance; short enough that a grazing run's `FOOD` counter climbs and goes rather than sitting there (a merge does not restart it).                                                                                                        |
| `FLOATER_FADE_FRACTION`                   | 0.3   | ×        | The last 30 % of the lifetime fades.                                                                                                                                                                                                                                           |
| `FLOATER_MAX_VISIBLE`                     | 4     | floaters | The fourth push is where the column reaches the mass chip at the smallest cell; with one floater per cause (#443) it is also the count of `FLOATER_CAUSE`, so the column cannot reach it by repetition.                                                                        |
| `ZONE_PILL_SECONDS`                       | 4     | s        | `HINT_DURATION_SECONDS`'s value: the zone pill is read once, like a hint.                                                                                                                                                                                                      |
| `ZONE_PILL_COOLDOWN_SECONDS`              | 20    | s        | A cell grazing a zone boundary is told once.                                                                                                                                                                                                                                   |
| `ZONE_PILL_DOT_PX`                        | 8     | px       | The zone dot, as the audit frame.                                                                                                                                                                                                                                              |
| `RELATION_RING_RADII`                     | 1.3   | × r      | `ENGULF_WARNING_RING_RADII`'s value, so every relation ring sits at one distance.                                                                                                                                                                                              |
| `RELATION_RING_MIN_GAP_PX`                | 6     | px       | Ring outside the membrane on a small far cell.                                                                                                                                                                                                                                 |
| `RELATION_RING_STROKE_PX`                 | 1.5   | px       | Thinner than the 2 px threat ring: still solid lines against its dashed, rotating band (`WARNING_RING_DASH_PX`, `WARNING_RING_STROKE_PX`).                                                                                                                                     |
| `TOXIC_RING_LINE_GAP_PX`                  | 2     | px       | The gap between the toxic ring's two lines: a double line reads as double at 1.5 px strokes, the single edible line never does (principles-and-palette.md §2).                                                                                                                 |
| `EDIBLE_RING_ALPHA`                       | 0.6   | ×        | Sixteen `GAIN` rings at full strength (14.4:1 on the field) would outshine the dish around a big cell's prey (ui-type.md §7); at 0.6 `GAIN` blends to 5.78:1 on `BG_FIELD`, still over `RIM_MIN_CONTRAST` 4.5. #385 brings evidence of `RELATIONS_MAX_RINGED` rings on screen. |
| `TOXIC_RING_ALPHA`                        | 0.9   | ×        | `DANGER` is darker than `GAIN`, so it cannot share 0.6 (2.76:1 on the field, 1.87 over the shallows, 2.45 over the vent). At 0.9 it blends to 4.90:1 on `BG_FIELD`, over the 4.5 floor (0.85 gives only 4.48). Toxic rings are few, so the dish stays the brightest thing.     |
| `MASS_TREND_WINDOW_SECONDS` (**state**)   | 1     | s        | The net rate over a second: a single eat does not flip the trend, a sprint does.                                                                                                                                                                                               |
| `MASS_TREND_ENTER_PER_SECOND` (**state**) | 0.2   | mass/s   | The net rate's size at which the chip leaves `steady`; the broth decay of a mass-120 cell is the first to reach it.                                                                                                                                                            |
| `MASS_TREND_EXIT_PER_SECOND` (**state**)  | 0.1   | mass/s   | The size under which it returns to `steady`: half the entry, so a rate sitting on 0.2 does not flicker the arrow or restart the `shrink` hold.                                                                                                                                 |
| `RELATIONS_MAX_RINGED` (**state**)        | 16    | cells    | Rings on the nearest 16 on-screen cells: the budget line (rendering/own-cell-indicators.md §10).                                                                                                                                                                               |
