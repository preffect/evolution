{
  "name": "evolution",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Reusable multiplayer game template (client/server, native WebSocket multiplayer, MCP game-state visibility)",
  "scripts": {
    "dev:server": "pnpm --filter @evolution/server dev",
    "dev:client": "pnpm --filter @evolution/client dev",
    "dev": "pnpm dev:server & pnpm dev:client",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:watch": "vitest",
    "lint": "eslint . && prettier --check .",
    "lint:fix": "eslint . --fix && prettier --write .",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "generate:balance": "pnpm --filter @evolution/shared build && node scripts/generate-balance.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@vitest/coverage-v8": "^3.0.0",
    "angular-eslint": "^21.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-sonarjs": "^4.2.0",
    "eslint-plugin-unicorn": "^74.0.0",
    "jscpd": "^4.0.0",
    "pixelmatch": "^7.0.0",
    "pngjs": "^7.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  }
}
