node_modules/
dist/
.turbo/
*.tsbuildinfo

# Dev artifacts
*.log
.DS_Store

# Environment
.env
.env.local

# IDE
.idea/
*.swp
*.swo

# pnpm store (local package cache)
.pnpm-store/

# Runtime files
.game-logs/
.game.pid

# Game data (runtime); data/balance.json is generated from the shared constants and committed
data/*
!data/balance.json

# Angular cache
.angular/

# Coverage output (the gate prints a summary; reports are never committed)
coverage/

# Devcontainer build artifact
.devcontainer/.build-checksum

# Claude personal notes
CLAUDE.local.md

# QA screenshots and agent logs (baselines live in qa/baselines and ARE committed)
.qa/

# Replays written by failing gameplay scenarios (docs/TESTING.md §8); reproduction artifacts, never committed
qa/replays/

# Agent worktrees (scripts/agent.sh --branch)
.worktrees/

# Python bytecode (scripts/github)
__pycache__/
*.pyc
# Generated audio (docs/AUDIO-PIPELINE.md §3): the manifest is committed, the files are not
assets/audio/*.mp3
assets/audio/*.ogg
assets/audio/*.wav
